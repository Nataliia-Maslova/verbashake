streamlit[auth]>=1.42.0
# Pinned exact, not >=: Authlib 1.6.6+ breaks st.login()'s Tornado integration
# (FrameworkIntegration.set_state_data writes to `session` even when a cache
# is configured; Streamlit's TornadoOAuth2App passes session=None assuming it
# wouldn't) -- 400 "'NoneType' object does not support item assignment" on
# /auth/login. See streamlit/streamlit#13461. 1.6.5 is confirmed working.
Authlib==1.6.5
plotly>=5.18.0
pandas>=2.0.0
openpyxl>=3.1.0
gTTS>=2.4.0
rapidfuzz>=3.6.0
numpy>=1.26.0
gspread>=6.0.0
google-auth>=2.0.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
stripe>=10.0.0
google-genai>=1.0.0
faster-whisper>=1.0.0
diskcache>=5.6.0
sentence-transformers>=2.6.0
nltk>=3.8.0
PyYAML>=6.0
