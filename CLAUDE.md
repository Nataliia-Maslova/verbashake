# VerbaShake — рабочий бриф для Claude Code

Этот файл — точка входа для Claude Code в этом репозитории. Здесь собраны решения,
принятые в обсуждении с Наталией (владелицей проекта) поверх того, что уже написано
в `IMLLS_App_Store_Roadmap.docx` и `REFACTOR_PROMPT.md`. Прочитай сначала их — они
дают полную картину архитектуры и уже проработанный план. Этот файл их не заменяет,
а уточняет и переводит в конкретные задачи.

## Контекст проекта

VerbaShake (изначально IMLLS) — мультиязычное приложение для изучения языков
(английский, украинский, испанский, корейский), сейчас на Streamlit, живёт на
verbashake.streamlit.app. Цель — довести до продакшна и монетизировать (freemium-
подписка), в перспективе — публикация в Google Play / App Store.

Второй репозиторий `imlls` (github.com/Nataliia-Maslova/imlls) — опубликованная
дипломная версия с 3 собственными GEC-моделями (T5, en/es/ko). Этот репозиторий
(`verbashake`) — недоработанный вариант с интеграцией Gemini API вместо GEC-моделей.
Работаем именно с этим репозиторием как с основой.

## Решения, принятые после написания roadmap-дока (важно — они уточняют/переопределяют его)

1. **GEC-модели (`engine/gec.py`) не используем вообще.** Наталья считает их
   недостаточно точными для монетизируемого продукта и не хочет тащить их вес в
   приложение. Grammar correction на платном тарифе — только через
   `engine/gemini.py::correct_grammar` (уже реализовано). `engine/gec.py` и связанные
   зависимости (transformers, torch, скачивание HF-моделей) можно убрать из
   `requirements.txt` и деплоя.

2. **Открытый вопрос дока про Phase 1/3/4 — решён: они платные.** `warmup_question`,
   `generate_practice_test` (live-регенерация), `chat_with_tutor` вызывают Gemini
   вживую и должны попадать под платный тариф — иначе принцип "free = zero live AI"
   из дока сам себе противоречит.

3. **YouTube-ссылки — не динамический поиск, а курируемый статический список.**
   Убираем `find_youtube_videos` (живой вызов YouTube Data API) и связанные с ним
   `generate_youtube_query` / `get_lesson_topic`, используемые именно для видео-
   поиска. Вместо этого — простая таблица (например `data/youtube_links.csv`:
   url, language, level, topic), наполняется вручную/куратором. Ноль вызовов API на
   рантайме.

4. **`engine/curriculum.py` и `engine/adaptive.py` — заменить на настоящий
   recommender.** Сейчас `curriculum.py` строит фиксированную линейную
   последовательность уроков (`advance()` просто +1 к индексу, без учёта
   успеваемости), а `adaptive.py` — RandomForest, который решает не "какую тему
   давать", а только "какую из 3 заготовленных последовательностей шагов
   использовать для конкретной фразы". Ни то, ни другое не решает задачу "куда вести
   пользователя по темам/уровню/типам ошибок, балансируя знакомое и чуть сложное".
   Новый модуль (`engine/recommender.py`) должен это делать.

   Для V1 — **не обучаемая модель, а прозрачная эвристика** (нет данных для
   тренировки на старте): скор кандидата на "что показать следующим" = приоритет по
   SRS (что просрочено к повторению) + разрыв в мастерстве по теме (что слабее всего)
   + бонус за умеренную новизну (чуть выше текущего уровня — ориентир: пользователь
   должен угадывать правильно ~80% случаев, не выше и не ниже). Обучаемую модель
   (contextual bandit / градиентный бустинг) имеет смысл вводить только после
   накопления реальных данных использования.

5. **Логи — облегчить.** `engine/logger.py` сейчас пишет в Google Sheets и локальный
   CSV буквально каждый шаг (timestamp, similarity, response_time_ms, attempts...).
   Наталье это не нужно. Вместо этого — компактная схема: вектор мастерства по
   темам/типам ошибок на пользователя, SRS-состояние (интервал/дата следующего
   повторения) на единицу контента, агрегаты для геймификации (XP, streak). Именно
   этот набор фич нужен recommender'у из пункта 4 — схему логов и схему
   recommender-фич проектировать вместе, не порознь.

6. **Платформа для первого продакшна — веб (без сторов), но собираем как PWA.**
   PWA-обёртка (manifest + service worker) — это просто способ упаковки, а не другая
   архитектура. Собираем фронт сразу как устанавливаемый PWA, но **не подаём в
   Google Play / App Store на первом этапе** — только веб + Stripe Checkout. Это
   убирает необходимость в RevenueCat / App Store & Play Billing и комиссии 15–30%
   на старте, и не требует прохождения ревью сторов. Публикация в сторах (тот же
   код, обёрнутый через Bubblewrap/Capacitor) — отдельный, более поздний этап, когда
   монетизация и retention подтвердятся.

7. **`engine/license.py` / `license_gate.py` — не подходит для подписки.** Это
   привязка лицензии к machine_id (одно устройство = один ключ), а не
   аккаунт-based доступ. Для веб-подписки через Stripe нужен обычный аккаунт
   (email/пароль или magic link) + статус подписки в БД, кросс-девайсный. Точку
   вызова `check_and_gate()` можно оставить как место интеграции, саму логику внутри
   — переписать.

## Проверено фактчекингом (16 августа 2026) — поправки к доку

- **Дедлайн Gemini 2.5 Flash / Flash-Lite (в доке — 16 октября 2026) не подтверждён.**
  На официальной странице https://ai.google.dev/gemini-api/docs/deprecations на
  момент проверки указано "no shutdown date announced" для обеих моделей. Похоже,
  источник спутал их с реально анонсированным отключением gemini-2.5-pro /
  gemini-3-flash. `_FLASH` / `_LITE` в `engine/gemini.py` и так вынесены в
  константы — менять по факту официального анонса, не по календарю. Не считать
  горящим дедлайном.
- **Дедлайн Google Play по target API level (31 августа 2026) — подтверждён,
  реальный.** Новые приложения с этой даты должны таргетить Android 16 (API 36),
  есть возможность отсрочки до 1 ноября. Актуально только на этапе сборки TWA для
  Google Play (см. фазу F ниже) — сейчас, пока публикации нет, не блокирует.

## План работ (по фазам, делать по порядку; между фазами — короткий чек-ин с Наташей, не проходить всё молча)

### Фаза A — Чистка стоимости (без изменения архитектуры, самая быстрая экономия)
- Добавить persistent-кеш (diskcache/SQLite) для генеративных вызовов в
  `engine/gemini.py`, ключ — (язык, уровень, тема, тип задания[, хеш фразы]).
- `check_practice_answer` — для `test_type == "multiple_choice"` сверять через
  `engine/scorer.py` (rapidfuzz), Gemini оставить только для свободного текста и
  перевода.
- Убрать `find_youtube_videos` / `generate_youtube_query` / `get_lesson_topic`
  (используемые для видео) — заменить на статическую таблицу `data/youtube_links.csv`
  (см. пункт 3 выше).
- Удалить `engine/gec.py` и связанные зависимости из `requirements.txt`.

### Фаза B — STT: стоимость и надёжность — ГОТОВО (2026-08-16)
- `engine/stt.py` переписан на `faster-whisper` (модель `small`, CPU, int8-квантизация,
  `WHISPER_MODEL` в secrets/env — переопределяемо). Публичный интерфейс не изменился
  (`transcribe_bytes`, `transcribe_file`, `whisper_available`), вызывающий код в
  `grammar.py`/`reading_app.py` не тронут — `wh_lang`/`WHISPER_LANG` в приложении и
  так уже были обычными ISO-639-1 кодами (`en`/`uk`/`es`/`ko`...), ровно тем форматом,
  который принимает faster-whisper, — конвертация не понадобилась.
- Решение faster-whisper vs Gemini аудио-вход — выбрано в пользу faster-whisper
  (Наталья подтвердила): ноль предельной стоимости за вызов и не растит трафик на
  Gemini API (см. Фазу A). `google-cloud-speech` убран из `requirements.txt`,
  `faster-whisper` добавлен. `packages.txt` уже содержал `ffmpeg` — ничего менять не
  пришлось.
- **Не проверено вживую** (нет доступа к реальному Gemini/аудио в песочнице
  разработки): фактическую задержку транскрипции на Streamlit Cloud и достаточность
  памяти для модели `small` на бесплатном тарифе — стоит промерить после деплоя. Если
  `small` окажется медленной/тяжёлой — понизить `WHISPER_MODEL` до `base` через
  secrets без правки кода.

## Живая проверка фаз A+B (2026-08-16)

Прогнала локально (Python 3.11, venv, полный `requirements.txt`) всё, что не требует
`GEMINI_API_KEY` или реального микрофона:

- **STT (Фаза B) — подтверждено по-настоящему**: синтезировала фразы через gTTS на
  en/uk/ko, прогнала через `transcribe_bytes`/`transcribe_file` — распознавание точное
  во всех трёх (единственная разница — знаки препинания). Задержка ~1–3 с на короткую
  фразу при уже скачанной модели `small` на CPU.
- **Приложение целиком (`streamlit run app.py`) стартует и рендерится** без ошибок:
  главное меню, переключение между модулями (Grammar/Vocabulary/Reading/My Phrases),
  выбор урока — всё работает. Reading-модуль (фонетика/произношение) доходит до
  экрана записи голоса без единого вызова Gemini.
- **Grammar-модуль дальше шага 1 не прошла** — Phase 1 (Warmup) обязательный
  (`REQUIRED_STEPS`) и сразу вызывает Gemini; без реального ключа дальше по требуемому
  флоу не пройти. Это не регрессия Фазы A/B, так было и в оригинальном коде.
- **Найдены и исправлены 2 реальных бага** (не входили в план фазы A/B, но напрямую
  блокировали проверку и являются реальным риском для любого окружения без
  `.streamlit/secrets.toml`, например деплоя только через env-переменные):
  `st.secrets.get(...)` в Streamlit кидает `StreamlitSecretNotFoundError`, если файла
  `secrets.toml` вообще нет (это не как обычный `dict.get()`, там `KeyError` не при
  чём) — обёрнуто в `try/except` в `engine/stt.py::_get_model()` и
  `engine/gemini.py::_configure()`. Раньше в `_configure()` это ломало и намеренный
  фолбэк на переменные окружения (`or os.environ.get(...)`), даже если ключ реально
  был в env, а `secrets.toml` просто отсутствовал.
- **Не проверено** (нужен реальный `GEMINI_API_KEY` и/или живой микрофон — вне
  досягаемости песочницы): фактическое попадание в кеш при повторном вызове,
  содержательный вывод `correct_grammar`/`generate_practice_test`/`warmup_question`,
  прохождение multiple_choice-теста через реальный сгенерированный тест, запись голоса
  через `st.audio_input` в браузере.

### Фаза C — Recommender и облегчённый прогресс — ГОТОВО, живая БД (2026-08-16)

**Решения, принятые в чате перед реализацией** (важно помнить контекст):
1. Хранилище — забежали вперёд в Фазу D: настоящая Postgres БД (рекомендовала
   Supabase — Postgres + встроенный Auth в одном месте, пригодится для аккаунтов
   в Фазе D), а не Google Sheets и не локальный SQLite (обнулился бы на каждом
   Streamlit Cloud редеплое).
2. Recommender решает и МОДУЛЬ (grammar/vocab/reading), и тему — полная замена
   `curriculum.py`, не только внутримодульный выбор.
3. Теги для Vocabulary (нет уровня) и Reading (нет ни темы, ни уровня) —
   Наталья тегирует вручную через CSV-шаблоны (см. ниже), как с YouTube-CSV в Фазе A.

**Что сделано:**
- [schema.sql](schema.sql) — `content_units` (реестр уроков, единый ID вида
  `grammar:42` / `vocab:Food:12` / `reading:en:7`, гранулярность УРОКА, не фразы —
  так совпадает с тем, что реально запускает UI), `mastery` (EMA правильности по
  теме), `srs_state` (SM-2-lite интервалы повторения), `lesson_pointer` (замена
  "прогресса" из Sheets, ключ `user+target_lang+module` — grammar/vocab/reading
  независимо, как раньше через суффикс `language_pair`), `gamification`
  (перенесённый `gamification.csv` — та же проблема с обнулением на редеплое,
  что чуть не случилась с SQLite, только на CSV, только раньше).
- [engine/db.py](engine/db.py) — подключение через SQLAlchemy, `DATABASE_URL` в
  secrets/env. Универсальный upsert-хелпер.
- [engine/recommender.py](engine/recommender.py) — скор кандидата = `0.5·SRS-
  просрочка + 0.3·(1-mastery) + 0.2·новизна` (веса — константы, не обучены, по
  плану из решения #4). Публичное API: `get_next`, `record_result`,
  `get_stats`, `get_pointer`/`save_pointer`, `reset_user`.
- `engine/gamification.py` — переключен на БД с автоматической разовой миграцией
  из старого CSV при первом чтении пользователя (реальные данные `student1`,
  1490 XP, не потеряются). Если `DATABASE_URL` не настроен — тихо работает на
  CSV как раньше (не ломает то, что уже было, до момента как БД поднимется).
- [scripts/seed_content_units.py](scripts/seed_content_units.py) —
  `--generate-templates` создаёt [data/vocab_tags_template.csv](data/vocab_tags_template.csv)
  (29 строк, по одной на тематический лист) и
  [data/reading_tags_template.csv](data/reading_tags_template.csv) (273 строки —
  English/Ukrainian/Spanish/Korean, остальные 8 языков можно добавить тем же
  форматом позже). Без флага — сидирует `content_units`: Grammar тегируется
  автоматически (`topic_en` + `difficulty`→CEFR из `imlls_database_with_titles.xlsx`,
  173 урока), Vocab/Reading берут level/topic из заполненных шаблонов (553 / 273
  урока — числа сверены с реальным UI). **Важно про Vocab**: lesson_id там не
  сырой из Excel, а глобальный ID, который рантайм строит через
  `vocab_loader._build_global_index()` — сид-скрипт использует ту же функцию,
  иначе ID бы не совпали с тем, что реально открывает приложение.
- Переключены на recommender: `grammar.py`, `reading_app.py`, `path_app.py`
  (полностью переписан — "Coming up" теперь живой снэпшот топ-6 кандидатов, а
  не фиксированная последовательность: прохождение верхнего урока может
  переставить остальные при следующей отрисовке), `app.py` (карточки прогресса
  на лаунчере), `engine/picker.py`, `custom_app.py`, `engine/session.py`.
- **Retired**: `engine/curriculum.py`, `engine/adaptive.py` (тот самый toy
  RandomForest на &lt;25 семплах — он решал "сколько раз повторить фразу", а не
  тему/уровень, что бриф и просил заменить; полноценной замены этому конкретному
  куску нет, теперь всегда полная 8-шаговая последовательность). Из
  `engine/logger.py` убрано всё, кроме `_get_spreadsheet()` — она нужна
  `custom_store.py` для своих целей (не связано с recommender).
- Найден и починен попутный баг: как в Фазе B, `st.secrets.get(...)` кидает
  исключение (не просто отсутствие ключа), если файла `secrets.toml` нет вообще.
- Логика проверена автономно (без реальной БД — её у меня нет): парсинг unit_id,
  скоринг-формула на синтетических данных (слабая тема приоритетнее сильной,
  просроченное повторение — выше всего, ведёт себя ожидаемо), числа сидирования
  сверены с реальными данными в UI (Grammar 173, Vocab 553, Reading 273 —
  совпадает с тем, что видели на скриншотах).

## Живая проверка Фазы C (2026-08-16, продолжение)

Наталья подняла Supabase и прогнала шаги выше. Проверила вживую на реальной БД:

- **Подключение**: с первой строки (direct connection, `db.xxx.supabase.co`) не
  сработало — `could not translate host name`. Новые проекты Supabase резолвят
  прямой хост только по IPv6, большинство сетей — IPv4-only. Фикс: строка из
  вкладки **Session pooler** (хост вида `aws-0-<region>.pooler.supabase.com`,
  юзернейм `postgres.<project-ref>`) — именно то, что изначально рекомендовала.
  Стоит иметь в виду при следующем деплое (Streamlit Cloud может иметь тот же
  IPv6-нюанс).
- `schema.sql` прогнан, все 5 таблиц созданы.
- `seed_content_units.py` (без флага) реально записал в БД: 999 юнитов —
  173 grammar (все протегированы, level из `difficulty`) + 553 vocab + 273
  reading (vocab/reading пока без тегов — шаблоны не заполнены, это ожидаемо).
- **Найден и починен реальный баг холодного старта**: для юзера без истории
  рекомендер сначала предлагал грамматику уровня B2 вместо A1 — потому что
  непройденная тема получала нейтральный приор mastery=0.5 ("уже неплохо"), и
  бонус за новизну тянул к чуть более сложному материалу. Поправила: для темы
  без единой попытки приор теперь 0.0 ("ничего не знаю"), а цель новизны — сам
  A1-уровень (0.0), а не "чуть выше нуля" (иначе A2 обгонял A1 из-за маржи).
  Проверено эмпирически на реальных данных — топ-5 для нового юзера теперь
  ровно Grammar Lesson 1-5 (A1), как и должно быть.
- `record_result` проверен трижды подряд (правильные ответы на grammar:1):
  mastery 0 → 0.578125 (ровно EMA-формула, alpha=0.25), SRS-интервал 1→2→4 дня,
  urgency верно ушёл в 0 → урок выпал из топ-5, следующие по очереди (grammar:2..6)
  заняли его место. `reset_user` корректно чистит все три таблицы.
- Автомиграция `gamification.csv` → БД сработала при первом чтении: реальные
  данные student1 (1490 XP, 11 уроков, бейджи) перенеслись без потерь.
- Полный путь вживую через браузер: главное меню → "My Path" (карточка
  "Unit 1 / 806", совпадает с прямым запросом к БД) → рендер экрана с реальными
  данными (next lesson = Grammar "Describing things", A1, Lesson 1; "Coming up"
  = grammar:2..6) → клик "Start" → реальный переход в Grammar Lesson 1, шаг
  "Розминка" с живым вопросом от Gemini. Вся цепочка отработала без ошибок.

**Ещё не проверено**: содержательное поведение после того, как
`data/vocab_tags_template.csv`/`data/reading_tags_template.csv` будут заполнены
(сейчас vocab/reading участвуют в скоринге с `level=NULL`, т.е. нейтральным
приближением, а не реальным CEFR-уровнем) — рекомендации по ним станут точнее
после тегирования, но это можно делать постепенно, ничего не сломается.

## Теги контента и YouTube-подборка (2026-08-16, продолжение)

- **`data/vocab_tags_template.csv` заполнен** — 29 листов размечены по CEFR.
  Источник — не гадание: в удалённом `curriculum.py` уже была ручная разбивка
  этих же 29 тем по стадиям 1-6 (`VOCAB_STAGES`), просто перенесла на шкалу
  A1-C2. Один нюанс: лист "Basic" в старой схеме растягивался на стадии 1-3
  (диапазоны lesson_id 1-28/29-56/57-84) — моя схема тегирует целый лист одним
  уровнем, взяла нижнюю границу (A1). Если нужна разбивка "Basic" по под-урокам
  точнее — это отдельная доработка схемы (сейчас теги на уровне листа, не
  под-диапазона).
- **`data/reading_tags_template.csv` заполнен частично, осознанно**:
  `level` — у всех 273 строк (позиционный бакет по номеру урока по каждому из
  4 языков отдельно, т.к. общее число уроков разное: en=80, uk=65, es=54,
  ko=75). `topic` — только там, где были реальные данные: у английского и
  корейского листов есть колонка "Правило читання" (описание фонетического
  правила); заполнила topic из неё для 56 из 80 английских уроков (у корейского
  колонка есть, но пуста — 0 правил). Для украинского/испанского такой колонки
  нет вообще — matter of fact, там не тематическая лексика, а фонетика
  (буквосочетания, звуки), придумывать "темы" не стала, оставила пустым, чтобы
  не выдавать догадку за данные.
- Перезалито в Supabase — проверено напрямую: grammar 173/173 (level+topic),
  vocab 553/553 (level+topic), reading 273/273 level / 56/273 topic.
- **YouTube-пилот на английском**: `data/youtube_links.csv` заполнен 8 видео на
  все 6 CEFR-уровней (A1×2, A2, B1, B2, C1×2, C2) — реальные ссылки, найдены
  браузером на youtube.com (не выдуманы), отобраны по высоким просмотрам
  (171K–18M) и специализированным ESL-каналам (без рекламных/спонсорских
  результатов). Проверено программно через `youtube_links.get_videos()` — матчинг
  по языку+уровню срабатывает корректно на всех 6 уровнях. **Наталья ещё не
  оценила качество подборки** — по решению из чата это пилот на одном языке;
  остальные 3 (uk/es/ko) делать только после её одобрения этого набора.

### Фаза D — Монетизация — КОД ГОТОВ, ждёт настройки Google/Stripe (2026-08-16)

**Решения, принятые в чате перед реализацией:**
1. LemonSqueezy (`engine/license.py`, `license_gate.py`, verbasake.lemonsqueezy.com) —
   подтверждено черновиком без покупателей, удалён полностью, не мигрирован.
2. Аутентификация — нативный `st.login()`/`st.user` (Streamlit ≥1.42, есть с 1.61)
   через Google как OIDC-провайдера, а не Supabase Auth (Supabase — не
   OIDC-провайдер, для st.login() не подходит напрямую). Даёт настоящий
   cross-device логин с персистентной cookie (переживает обновление страницы,
   в отличие от голого `st.session_state`).
3. `user_id` теперь = `st.user.email` (реальный Google-аккаунт) — заменил
   свободный текст "Your name" во всех 4 местах, где он был (app.py, grammar.py,
   reading_app.py, custom_app.py).
4. Синхронизация подписки со Stripe — **опрос API**, не вебхуки: Streamlit не
   даёт HTTP-эндпоинт для приёма Stripe-событий без отдельной инфраструктуры.
   Статус кешируется в БД (`subscriptions`), перепроверяется через Stripe API
   при логине и когда кеш устаревает (`_STALE_AFTER = 1 час` в `engine/billing.py`).
   Значит отмена подписки может не отразиться до часа — осознанный компромисс
   для V1, апгрейд на вебхуки (например через Supabase Edge Function) —
   отдельная, более поздняя задача, когда появится реальная выручка.
5. Обсуждали более широкий вопрос — не переписать ли всё на другой фреймворк
   вместо Streamlit. Решили остаться: слишком много уже рабочего (recommender,
   Gemini, STT/TTS, геймификация), `st.login()` закрыл главный прежний
   аргумент "за переезд", и это соответствует общему паттерну проекта — не
   усложнять до того, как подтвердится монетизация. Единственное реальное
   больное место — PWA (Фаза E) даётся Streamlit через костыли, но не блокирует.

**Что сделано:**
- `schema.sql` — таблица `subscriptions` (user_id=email, stripe_customer_id,
  stripe_subscription_id, status, plan, current_period_end, checked_at).
- [engine/billing.py](engine/billing.py) — `is_paid()`, `create_checkout_session()`,
  `handle_checkout_return()` (обрабатывает редирект `?checkout=success&session_id=...`
  после оплаты), `_sync_from_stripe()` (опрос Stripe API).
- [engine/auth_gate.py](engine/auth_gate.py) — заменяет `license_gate.py`.
  `check_and_gate()` требует логин (не подписку — приложение бесплатно
  доступно залогиненным, просто без живой генерации Gemini). `current_user_id()`/
  `current_user_name()` — новый источник идентичности везде.
- `engine/gemini.py` — декоратор `@_require_paid` на ВСЕХ 10 публичных функциях
  (warmup_question, evaluate_warmup, correct_grammar, generate_practice_test,
  check_practice_answer, generate_speaking_task, chat_with_tutor, get_hint,
  suggest_alternatives, translate_phrase) — по принципу "free = zero live AI"
  из решения #2 фазы A/B (не только 3 явно названных в доке функции, а вообще
  всё, что реально зовёт Gemini). **Критичный нюанс**: декоратор написан НАД
  `@_cache.memoize()`, не под ним — иначе бесплатный юзер мог бы получить
  закешированный ответ платного юзера и проскочить гейт незамеченным.
  Проверено логическим тестом (не вживую): бесплатный юзер блокируется до
  похода в кеш, платный проходит.
- Все 13 мест в `grammar.py`, где реально зовётся Gemini, обёрнуты в
  try/except `PaidFeatureRequired` с единым `_show_upsell()` UI (кнопка "Go to
  Upgrade" → возврат на лаунчер, где уже есть кнопка апгрейда в сайдбаре).
  Полнота проверена не визуально, а AST-анализом кода — ни один вызов
  `_gemini.*` не остался вне try-блока. `reading_app.py`/`custom_app.py`/
  `path_app.py` Gemini вообще не вызывают — их трогать не пришлось.
- `requirements.txt`: `streamlit[auth]>=1.42.0`, `Authlib`, `stripe`.

**Нужно от Наташи, чтобы заработало вживую** (я не могу создать эти аккаунты
за неё):
1. **Google OAuth-клиент** (console.cloud.google.com → APIs & Services →
   Credentials → Create OAuth client ID → Web application). Authorized
   redirect URI должен совпадать 1-в-1 с тем, что пойдёт в secrets.toml.
2. В `.streamlit/secrets.toml` добавить:
   ```toml
   [auth]
   redirect_uri = "https://verbashake.streamlit.app/oauth2callback"  # или localhost:8501 для локальной разработки
   cookie_secret = "любая случайная длинная строка"
   client_id = "из Google Cloud Console"
   client_secret = "из Google Cloud Console"
   server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
   ```
3. **Stripe**: завести аккаунт (stripe.com), создать Product + recurring Price
   (месячная подписка), взять Secret Key и Price ID.
4. В `.streamlit/secrets.toml` добавить:
   ```toml
   STRIPE_SECRET_KEY = "sk_..."
   STRIPE_PRICE_ID = "price_..."
   ```
5. Прогнать обновлённый `schema.sql` (новая таблица `subscriptions`) — то же
   самое, что и в Фазе C, просто ещё раз (`CREATE TABLE IF NOT EXISTS`,
   безопасно перезапускать).

**Не проверено вживую** — нужны реальные Google/Stripe аккаунты, которых у
меня нет: сам `st.login()` флоу (редирект, cookie, возврат в приложение),
реальный Stripe Checkout (оплата, редирект назад, синхронизация статуса),
поведение `_show_upsell()` в реальном UI при попытке бесплатного юзера дойти
до Gemini-функции.

## Живая проверка Фазы D (2026-08-20)

Наташа настроила Google OAuth-клиент и Stripe (test mode) сама, прогнала
`schema.sql` для таблицы `subscriptions`. Проверено вживую:

- `[auth]` секция в secrets.toml собрана правильно (изначально был баг —
  пропущен заголовок `[auth]`, из-за чего `client_id`/`client_secret`
  Google OAuth падали в ту же секцию, что и `[gcp_service_account]`, с
  конфликтом ключей — поправили вместе).
- Логин-гейт: редирект на настоящую страницу Google, узнаёт приложение
  ("to continue to Verbashake") — OAuth-клиент настроен верно.
- `create_checkout_session()` создаёт настоящую тестовую Stripe-сессию,
  страница оплаты рендерится с бейджем Sandbox, ценой 8.88 EUR/мес.
- Наташа прошла оплату тестовой картой по-настоящему (свой терминал,
  `streamlit run app.py` + localhost:8501) — **но статус остался Free**.

**Найден и починен реальный баг** (не выдумка, не догадка — воспроизвела и
исправила на её настоящей подписке): `engine/billing.py::_sync_from_stripe`
читал `sub.current_period_end` с самого объекта Subscription — в свежих
версиях Stripe API это поле переехало на уровень позиции подписки
(`sub['items']['data'][0]['current_period_end']`). Обращение падало с
`AttributeError`, тихо проглатывалось внешним `try/except` в
`handle_checkout_return()`, лог уходил только в её терминал (не в UI) — со
стороны выглядело как "ничего не произошло", хотя Stripe-сессия была
`status: complete, payment_status: paid`. После фикса синхронизация
проверена на её реальном `customer_id` — статус в БД стал `active`,
`is_paid()` → `True`, в приложении подтверждено (Premium показывается).

**Известный побочный эффект бага** (не исправляла без разрешения): у неё
теперь 2 отдельных Stripe-клиента с одной почтой (`cus_V6cwPtZ5DgiIqd`,
`cus_V6cuwuOtMdbxk7`) — код создавал нового клиента при каждой попытке,
потому что первая не записалась в БД и `create_checkout_session()` не
находил существующий `stripe_customer_id`. В test mode не критично; если
нужен порядок — отменить лишнюю тестовую подписку в Stripe Dashboard
вручную.

Наташа подтвердила: после апгрейда до Premium warmup-вопрос (Phase 1) реально
генерируется через Gemini — `@_require_paid` пропускает платного юзера,
`_show_upsell()` для платного пути, разумеется, не показывается. Полный цикл
(логин → оплата → Premium → живая генерация) подтверждён от начала до конца.

**Ещё не проверено**: `_show_upsell()` со стороны бесплатного юзера (что видит
тот, кто НЕ платил, при попытке дойти до Gemini-функции) — раз основной платный
путь уже подтверждён, это низкий риск, но для полноты стоит завести второй
Google-аккаунт без подписки и проверить экран апгрейда, когда будет время.

## Фичи/правки, обсуждённые перед Фазой E (2026-08-20)

**Статус на 2026-08-21**: пункты 2/3/5/6 реализовано и проверено вживую (не
через UI-клики — см. «Реалізація пунктів 2/3/5/6» ниже, ближе к Фазе E).
Пункт 1 — репозиторий открыт, интеграция не выбрана. Пункт 4 — книги открыты,
паттерны описаны, в промпт ещё не зашиты. Пункт 7 всё ещё ждёт списка каналов
от Натальи.

Наталья хочет доработать ряд фич перед PWA-упаковкой. Ниже — 7 пунктов из
обсуждения, каждый с моим разбором. Это план разговора, не готовые решения —
следующая сессия должна начать с уточнения открытых вопросов, а не с кода.

**1. "Taste Skill" (github.com/Leonxlnx/taste-skill) — дизайн-инструмент.**
Наталья подтвердила: узнала о нём из университета, доверяет источнику, можно
посмотреть репозиторий в следующей сессии (я его ещё не открывала — только
видела рекламный пересказ, само репо не проверено). Альтернатива без стороннего
инструмента: доработать существующую дизайн-систему Mova (`static/mova/`)
напрямую своими средствами — обсудить, какой путь выбрать, после того как
посмотрим репо.

**2. Разминка (warmup) — больше тем + двуязычный показ вопроса на A1.**
- Список тем в `engine/gemini.py::warmup_question()` сейчас захардкожен (5 тем)
  — расширить тривиально, для всех языков одинаково (темы передаются в промпт).
- Уточнено Наташей: на A1 показывать **сам вопрос/задание** на родном и
  изучаемом языке одновременно — отвечает ученик всё равно на изучаемом.
  Не нужно два отдельных вызова Gemini (дороже) — один промпт, просящий вернуть
  вопрос на обоих языках сразу. Сделать переключаемым (потом можно оставить
  двуязычно или убрать родной) — не жёстко зашивать в промпт.

**3. Тест на уровень при входе (опционально).**
Хорошо ложится на recommender из Фазы C: результат теста может **засеять
начальный mastery** по темам вместо старта с 0.0 ("ничего не знаю") для
каждого юзера — тогда лёгкий материал получает высокий mastery и низкий
приоритет в рекомендациях (не блокируется жёстко, но и не навязывается).
Открытый вопрос на обсуждение: полноценный адаптивный тест (вопросы
подстраиваются по ходу) или простая самооценка/короткий диагностический квиз
(5-10 вопросов на модуль)? Учитывая принцип проекта "простая эвристика в V1,
усложнение — потом" — я склоняюсь к короткому квизу, не адаптивному движку,
но это на усмотрение Наташи.

**4. Внешние сайты с упражнениями (agendaweb.org и т.п.) + 3 её книги
(Голицынский, Murphy's English Grammar in Use, Cambridge Vocabulary in Use).**
⚠️ **Авторское право**: это коммерчески издаваемые защищённые учебники —
копировать их реальные упражнения (предложения, тексты) в платный продукт —
юридический риск. То же самое со скрейпингом agendaweb.org — нельзя
republish чужой контент. **Легальный путь**: использовать книги/сайты как
источник **форматов/паттернов** упражнений (какие типы заданий существуют на
конкретное грамматическое явление — пропуск с артиклем, форма глагола,
трансформация предложения) и зашить эти паттерны в промпт для Gemini, чтобы
он генерировал НОВЫЕ оригинальные упражнения в этом стиле — не копировал
существующие. Прямая связка с пунктом 5. Файлы книг лежат в
`~/Downloads/*.pdf` — в этой песочнице не открылись (EPERM), в следующей
сессии стоит попробовать снова, если понадобится посмотреть конкретные
страницы (только для анализа формата, не для копирования контента).
Для не-английских языков — нужно искать эквивалентные форматы отдельно, или
полагаться на промпт без книги-образца.

**5. Тесты в практике слишком узкие + промпт не различает грамматику/лексику.**
Естественное расширение Фазы C: сейчас `generate_practice_test` берёт только
`session.phrases()` (только текущий урок) — можно подмешивать материал из тем
с низким mastery или просроченным SRS-повторением через уже существующие
`engine.recommender` данные, не отдельная система. Плюс разделить промпт по
цели: тест на грамматику → фокус на конструкции/окончания/формы глагола; тест
на лексику → фокус на правильное словоупотребление в контексте. Сейчас один
общий промпт для всех модулей — нужно разделить.

**6. Коррекция ошибок — показывает только 1 ошибку, нужно все + формат
зачёркнуто/правильно + выбор пользователем, что отрабатывать.**
Важно: JSON-схема `correct_grammar`/`evaluate_warmup` в `engine/gemini.py` УЖЕ
возвращает список ошибок (`"errors": [...]`), не одну — значит проблема или в
промпте (Gemini находит только одну и не ищет остальные — промпт можно явно
попросить искать все), или в UI grammar.py (рендерит только первую из списка).
Нужно проверить в новой сессии, что именно. Формат "~~было~~ **стало**" —
обычный Streamlit markdown, просто. Выбор пользователем, какие ошибки
отрабатывать — доработка существующего `_collect_error`/`_phase_error_review`
flow (сейчас идёт по всем ошибкам подряд без выбора).

**7. Больше видео, для всех языков.** Наталья сама подберёт каналы для
uk/es/ko (пилот на английском из Фазы A уже одобрен) — просто ждём её список,
заполнить `data/youtube_links.csv` тем же способом.

## Реализация пунктов 2/3/5/6 + разведка 1/4 (2026-08-21)

Наталья подтвердила направление по всем 4 открытым вопросам и попросила сразу
реализовать пункты 2/5/6 в этой сессии. Порядок ниже — по пунктам из
предыдущего раздела.

**Пункт 1 (Taste Skill) — разведано, не интегрировано.** Репозиторий открыт
(github.com/Leonxlnx/taste-skill, MIT, 78.7k★, реальный и активный — не
подозрительный). Это набор Claude-Code-скиллов для промпт-инженерии
фронтенд-кода (layout variance, motion, spacing, design-system inference), а
не runtime UI-библиотека. Наиболее применим к CSS-работе над `static/mova/`
(написание/ревью нового CSS), почти не применим к самому Streamlit-каркасу
(компоненты генерируются фреймворком, не пишутся вручную как JSX/HTML).
Решение, какой путь выбрать (поставить скилл через `npx skills add` или
просто дорабатывать Mova вручную) — ещё не принято, это отдельное
обсуждение дизайна.

**Пункт 4 (книги для форматов упражнений) — разведано, не зашито в промпты.**
`poppler` не был установлен (поэтому раньше падало с EPERM на рендер
страниц) — поставила через `brew install poppler`, после этого `pdftotext`
вытащил текст без проблем из всех трёх файлов в `~/Downloads/`. Извлечена
ТОЛЬКО структура заданий, ни одного реального предложения/упражнения из книг
никуда не скопировано:
- **Голицынский**: тема → короткое правило → "Упражнение N" → одна
  инструкция ("Вставьте артикль где необходимо" / "Переведите") →
  пронумерованные предложения с пропусками, сложность растёт внутри раздела.
- **Murphy (Grammar in Use)**: разворот на 2 страницы — левая: объяснение +
  примеры; правая: упражнения с подномерами (4.1, 4.2...), типы — "поставь
  глагол в правильную форму" (пропуск + слово-подсказка в скобках),
  трансформация предложения, множественный выбор.
- **Cambridge Vocabulary in Use**: подномера на юнит (6.1, 6.2...), разные
  типы — подпись к диаграмме/карте, "проверь себя" клоуз без подглядывания,
  заполнение предложений целевой лексикой, "Over to you" — открытые
  персональные вопросы по теме юнита.
Следующий шаг (не сделан) — зашить эти паттерны в промпт
`generate_practice_test` как описание ФОРМАТА задания, отдельно для каждого
test_type; для языков помимо английского паттерны нужно искать отдельно или
полагаться на промпт без книги-образца.

**Пункт 2 (warmup: больше тем + двуязычный показ на A1) — сделано.**
`engine/gemini.py::warmup_question()` — список тем расширен с 5 до 20
(hobbies, family, food, travel, routine, job/study, weather, movies, music,
transport, weekend plans, skills, neighborhood...). Добавлен параметр
`bilingual: bool` (по умолчанию выключен, НЕ зашит жёстко в промпт) — при
True функция возвращает `{"target": str, "native": str}` одним вызовом
Gemini (не двумя, как и просила Наталья). Функция теперь всегда возвращает
dict (раньше — строку) — обновлено единственное место вызова в
`grammar.py::phase1_warmup`. UI: чекбокс "🌐 Show the question in my language
too", по умолчанию включён на A1 и выключен на других уровнях, но всегда
переключается вручную — заблокирован после генерации вопроса (чтобы не
менять язык вопроса задним числом), разблокируется заново при
"Skip"/"Next". **Проверено вживую** через реальный вызов Gemini
(`gemini-2.5-flash-lite`, обход `@_require_paid` через `.__wrapped__` для
тестирования без логина): bilingual A1 вернул `{"target": "What do you eat
for breakfast?", "native": "Що ви їсте на сніданок?"}`; monolingual B1 —
новую тему из расширенного списка ("your favorite season and why") с
`native: None`.

**Пункт 5 (шире practice-тесты + разделение промпта grammar/vocab) —
сделано.** `engine/gemini.py::generate_practice_test()` — новый параметр
`module` выбирает одну из двух `_MODULE_FOCUS` инструкций в промпте: grammar
→ формы глагола/окончания/порядок слов/артикли/предлоги; vocab → правильный
выбор слова в контексте, структура предложения нарочно простая.
`grammar.py`: новый хелпер `_extra_practice_phrases()` берёт до 4 фраз из
`_recommender.get_next(module=...)` (слабые темы / просроченное
SRS-повторение вне текущего урока) и подмешивает их к `session.phrases()`
перед генерацией — тест больше не ограничен только текущим уроком.
**Проверено вживую**: одинаковые входные фразы ("I go to school" / "She has
a red pen") дали заметно разные тесты в зависимости от `module` —
grammar-тест спрашивал про спряжение/артикль ("He ___ to school" → goes; "I
have ___ red pen" → a), vocab-тест спрашивал про выбор слова в новом
контексте ("Every morning I go to ___" → work; "My brother has a ___
bicycle" → green). `_extra_practice_phrases()` проверен отдельно (read-only,
без записи в БД) — корректно подтянул 4 реальные фразы из других уроков
через `engine.loader`/`content_units`.

**Пункт 6 (только 1 ошибка → все ошибки + формат зачёркнуто/правильно +
выбор) — найден корень и сделано.** Причина была НЕ в промпте и НЕ в
Gemini — `correct_grammar()` всегда возвращал полный список `errors`
(проверено вживую: на "She have a red pen. He go to school yesterday."
вернулись ровно 2 отдельные ошибки с `fixed`/`explanation`/`native_prompt`),
но шаг 8 (`grammar.py`, "Make your own sentences") брал из ответа только
`["corrected"]` — полное исправленное предложение — а сам список `errors`
просто отбрасывал на уровне сбора (`results = [{"original": p, "corrected":
...["corrected"]}]`). Исправлено: `results` теперь хранит весь `correction`
dict. Рендер — каждая отдельная ошибка как `~~original~~ → **fixed**` +
пояснение-caption, с чекбоксом (по умолчанию включён) рядом. Кнопка
"Practice selected corrections" собирает отмеченные ошибки через
существующий `_collect_error(..., "step8", ...)` и показывает уже имеющийся
интерактивный retry-цикл `_phase_error_review("step8", ...)` (тот же
механизм, что и в warmup/practice/expression) — нового UI-компонента для
retry не понадобилось, только новый phase_key. "Complete Lesson" остался
незаблокированным отработкой (как и раньше) — практика выбранных ошибок
необязательна, просто доступна.

**Пункт 3 (тест на уровень при входе — короткий квиз) — сделано.** Новый
`engine/placement_quiz.py` — БЕЗ единого живого вызова Gemini (намеренно:
тест должен работать и для бесплатных юзеров, а не только Premium), оценка
локально через `engine.scorer.evaluate` (тот же rapidfuzz-матчинг, что и в
multiple_choice-практике). `build_quiz()` берёт по одному реальному
предложению из `content_units` на каждый имеющийся CEFR-уровень (A1..C1) для
выбранного модуля/языка через существующие `engine.loader`/
`engine.vocab_loader`, студент переводит native→target. `score_quiz()`
считает estimated_level = самый высокий уровень без пропусков снизу (не
засчитывает угаданный ответ после реального пробела), вызывает новый
`recommender.seed_mastery_from_level()` — массово проставляет mastery=0.6
для всех тем на/ниже estimated_level, которые юзер ещё ни разу не пробовал
(не перезаписывает реальные попытки). UI — необязательный expander на
лаунчере (`app.py::_render_placement_quiz`) под карточками модулей, доступен
сразу всем (не за paywall). **Проверено вживую на реальной БД** через
одноразового тестового юзера с последующей зачисткой
(`recommender.reset_user`, проверено — 0 строк осталось во всех 3
таблицах): квиз вытащил 4 реальных предложения (A1/A2/B1/B2) из реальных
grammar-уроков; смоделированы правильные ответы на A1/A2 и неправильные
дальше → estimated_level = A2, точно как и ожидалось; mastery засеялась на
0.6 для тем ≤A2; после этого `get_next()` для того же юзера выдал топ-5 =
B1-уроки (grammar:115-119) вместо только что засеянных A1/A2 — рекомендер
действительно снижает приоритет уже "известного" материала сразу после
квиза, без единой реальной попытки пользователя.

**Не проверено вживую через UI** — вход в приложение требует настоящего
Google OAuth-логина (`st.login()`), который я не выполняю сама (это действие
учётной записи пользователя). Локальный сервер (`streamlit run app.py`, уже
был запущен на :8501 с предыдущей сессии) подхватил все изменения и
рендерит логин-гейт без ошибок (проверено скриншотом + консоль браузера
чистая) — это подтверждает, что импорты и верхнеуровневый код в
`app.py`/`grammar.py` рабочие, но сам клик по чекбоксам/кнопкам в шаге 8,
warmup, квизе — не кликнут вживую. Наталья может проверить после логина;
если что-то в рендере выглядит не так — скажи, и я поправлю в следующей
сессии.

## Живая проверка через UI + 2 фикса от Натальи (2026-08-21, продолжение)

Наталья прошла квиз и шаг 8 вживую через настоящий логин.

**Баг 1 — `NameError: name 'target_lang' is not defined` на шаге 8 после
клика "Practice selected corrections".** Причина: `_phase_error_review("step8",
wh_lang, target_lang, native_lang)` использовал `target_lang`, а в `step8()`
такой переменной нет — там только `gec_lang` (языковой код, обычно
= `wh_lang`), из которого во всех остальных вызовах `_gemini.*` в этой же
функции строится `_gemini.lang_name(gec_lang)`. Поправлено — тот же паттерн.
Слепая зона моей проверки в конце прошлой сессии: я тестировала
`correct_grammar`/`_extra_practice_phrases` напрямую через Python, но не
кликала реальные кнопки step8 (вход требовал логина, который я не выполняю
сама) — эта конкретная строка ни разу не выполнилась до её живого клика.

**Баг 2 (по факту — не хватало фичи) — квиз на лаунчере сработал (mastery
засеялась), но не повлиял на стартовый урок при входе в модуль.** Наталья
предложила: если уровень по квизу достаточный — начинать с более сложных
уроков. Причина: экран выбора урока (`grammar.py::render_setup`) выбирает
стартовый урок только из `lesson_pointer` (где юзер остановился в прошлый
раз) — если пойнтера ещё нет (первый вход в модуль), по умолчанию всегда
урок №1, `engine.recommender` там вообще не спрашивался. Добавлен фолбэк:
если пойнтера нет, но по модулю ЕСТЬ реальный mastery-сигнал (новая
`recommender.has_signal()` — из квиза или из реальных попыток), стартовый
урок берётся из `recommender.get_next(module=...)[0]` вместо жёсткого №1, с
сообщением "💡 Recommended start: Lesson N (уровень) — based on your
placement quiz / activity". **Важный нюанс, найденный при проверке**: без
гейта `has_signal()` фолбэк работал бы и для совсем нового юзера без квиза —
но `get_next()` для полностью непройденного каталога не детерминированно
возвращает урок 1 (уроки одного уровня получают одинаковый score, сортировка
при равенстве не гарантирует lesson_id по возрастанию — в проверке чистый
новый юзер получил рекомендацию "Lesson 3" вместо 1). Гейт на `has_signal()`
убирает этот шум: для чистого cold start сообщение вообще не показывается,
поведение как раньше. **Проверено на реальной БД через одноразового
тестового юзера** (с зачисткой через `reset_user`): `has_signal()` = False
до квиза, True после `seed_mastery_from_level(..., "B1")`; `get_next()`
после этого корректно вернул урок уровня B2 (сложнее пройденного) вместо
урока 1.

## Второй фикс рекомендации: баннер вместо тихого фолбэка (2026-08-21, продолжение)

Наталья прошла квиз (оценка A2), но при входе в Grammar всё равно увидела
начало с урока 1 — и заметила, что в самом пикере уроков нигде не видно,
где A1, а где A2. Разобрала обе жалобы.

**Почему предыдущий фикс не сработал для неё**: он был написан как
`elif` — срабатывал ТОЛЬКО когда `lesson_pointer` вообще отсутствовал
(первый заход в модуль). У Натальи он уже есть — она реально проходила
Grammar Lesson 1 ещё в Фазе C/D (см. «Живая проверка Фазы C» выше), поэтому
`if progress:` всегда выигрывал первым, и `elif` с рекомендацией никогда не
выполнялся. Квиз в принципе не мог повлиять на её конкретный случай.

**Исправлено** (`grammar.py::render_setup`): убрала `elif`, рекомендация
теперь считается ВСЕГДА (если `_recommender.has_signal()` истинно), рядом с
уже посчитанным resume-пойнтером, а не вместо него — специально не
перезаписывает `default_idx` молча (мидлесон-прогресс не должен дёргаться
без спроса). Если рекомендованный урок отличается от того, что выбрал бы
resume-пойнтер — под ним показывается info-баннер + кнопка "Jump to N →",
клик по ней ставит `st.session_state["_jump_recommended_lid"]` и делает
`st.rerun()`; на следующем рендере это значение считывается один раз
(`pop`) и переопределяет `default_idx` под явным действием юзера.
**Проверено на реальной БД** через одноразового юзера, смоделировавшего
именно её сценарий (`save_pointer(..., "grammar:1", step=99)` — как будто
прошла урок 1, + `seed_mastery_from_level(..., "A2")` — как будто прошла
квиз): чистый resume-путь без баннера предложил бы урок 2 ("продолжай
после 1"), но `get_next()` с учётом A2-квиза вернул урок 115 (B1) —
баннер и кнопка "Jump to 115 →" должны появиться именно в этом случае.
Прибрано после проверки.

**Вторая жалоба — не видно границы A1/A2 в самом пикере уроков.**
Существующий фильтр "📚 Category" в вейв-навигаторе группирует уроки по
ТЕМЕ (`_GRAMMAR_CATEGORIES` — Basics/Nouns/Habits/...), не по CEFR-уровню —
там в принципе никогда не было информации об уровне. Добавлен
`recommender.lesson_levels(module)` — парсит CEFR-уровень каждого урока из
`content_units` через `unit_id` (не через `source_lesson` — у vocab там имя
листа, а не id урока, поэтому не годится). `engine/picker.py::
_render_flat_wave_nav` — новый параметр `level_map`, подпись каждого урока
теперь вида "Lesson 42 · A2" — видно и на подсказке при наведении на вейв, и
в дропдауне-фолбэке (оба источника читают один и тот же `lesson_names`).
**Проверено на реальной БД**: `lesson_levels("grammar")` вернул все 173
урока с правильными уровнями (сверено точечно: 1→A1, 83→A2, 124→B1,
147→B2 — совпадает с бакетами, которые видели в Фазе C).

## Аудит качества данных + прототип "конструкция + словарь по уровню" (2026-08-21, продолжение)

Наталья засомневалась, что жёсткое разбиение на 173 микро-грамматических урока
(imlls_database) — хорошая стратегия целиком (может быть скучно/долго), и
предложила альтернативный подход: не готовый список фраз на урок, а
конструкция (например "У меня есть") + слова по уровню ученика (только нужная
часть речи), причём подход должен обобщаться на любую конструкцию, включая ещё
не существующую в базе (пример — Conditional 2).

**Аудит данных** (только анализ, без изменений):
- vocabulary_translated.xlsx: 5 листов (`Verbs`, `Food`, `City`, `Hobbies`,
  `Restaurant` — 75% всего словаря, 3765/5015 строк) в `vocab_tags_template.csv`
  помечены уровнем **C2**, хотя внутри — бытовая лексика A1/A2 (almond, bake,
  arrive, avoid, in the shop...). Recommender фактически прячет 3/4 словаря от
  всех, кроме C2-пользователей.
- Слова внутри урока/листа расположены по алфавиту (не по частоте/теме/
  сложности) — Food lesson 1 = `almond → basket`, без связной темы.
- `lesson_name` в UI — автосгенерированное "Words: &lt;первые слова&gt;", не
  осмысленный заголовок (в отличие от чистых Grammar-топиков).
- Реальные дубли слов: "cake" дважды в одном и том же уроке (Food lesson 87,
  phrase_id 864/870) и 5 раз по разным урокам/темам — recommender не
  дедуплицирует между уроками.
- 26 полностью пустых строк (все языковые колонки — None) в паре листов.
- imlls_database: грамматика жёстко ограничена `difficulty` 1-4 → CEFR A1-B2
  (`DIFFICULTY_TO_CEFR` в scripts/seed_content_units.py) — ни одной темы C1/C2
  не существует физически.
- Каждый грамматический урок — ровно 7-8 фраз без исключений (уже частично
  компенсировано подмесом доп. фраз, Фаза C п.5).
- Не проверено системно, но замечено: "великий собака" (Grammar lesson 1, uk)
  — стоит выборочно вычитать переводы носителем.

**Вопрос про гранулярность "I have" (9+ уроков по лицам/формам) — логично, но
только для этой стадии.** Разбиение по лицам (I/you/we vs he/she,
утверждение/отрицание/вопрос — каждое отдельным уроком) — классический приём
аудиолингвального/структуралистского метода (Pimsleur/Callan): для полного
новичка с почти нулевым словарём нагрузка должна расти на одну переменную за
раз — смешать лица+словарь+порядок слов сразу означало бы захлебнуться. Плюс
"he/she + -s" — типичный источник ошибок, поэтому его осознанно тренируют
отдельно. Это оправдано именно на стадии, когда согласование по лицам само по
себе новый навык — но та же гранулярность уже избыточна там, где этот навык
давно автоматизирован (например "Actions done by others — present/past/future"
разбито на 3 темы просто по шаблону, хотя согласование по лицам к этому
моменту уже не проблема). Гранулярность должна сужаться по мере роста mastery
— это может решать recommender/новый движок конструкций, а не жёсткая
структура данных.

**Источник для обеих задач (пилот на английском, по решению Натальи) — CEFR-J
English Profile** (github.com/openlanguageprofiles/olp-en-cefrj, зеркало
cefr-j.org) — открытая лицензия (коммерческое использование бесплатно с
указанием авторства; C1/C2-словарь от Octanove — CC BY-SA 4.0). Файлы в
[data/cefr_j/](data/cefr_j/) (см. [data/cefr_j/README.md](data/cefr_j/README.md)
— атрибуция). Содержит Vocabulary Profile (headword, pos, CEFR level A1-B2 +
C1/C2-надстройка) и Grammar Profile (~250 структур с CEFR-J уровнем).

**9 подтверждённых пробелов в грамматике imlls** (сверено по Grammar Profile,
не на глаз) — список для будущего дописывания, по приоритету:
1. Reported/Indirect speech (He said that.../She asked if...) — A2-B1.
2. Relative clauses (who/which/that, defining/non-defining) — A2-B2.
3. Conditional 3 (If I had known, I would have...) — C1.
4. Modals of deduction (must/might/could have been) — B2/C1.
5. Used to / would для прошлых привычек — B1.
6. Wish + Conditional 2/3 (I wish I knew.../I wish I had known...) — B1-C1.
7. Phrasal verbs как категория — не встречается вообще.
8. Causative have/get something done — B1-B2.
9. it + be + adj + that/to-infinitive (It's important that.../It's necessary
   to...) — B1-B2.

(Более продвинутые inversion/cleft-конструкции — C1/C2, низкий приоритет для
V1, намеренно не включены.)

**Побочная находка**: Grammar lesson 139 "Hypothetical situations"
(difficulty 4) — это уже Conditional 2, просто без стандартного названия
(поэтому не нашли при беглом просмотре) — и с лишней смысловой добавкой:
каждый пример — 2 предложения (условное + "however, ..." объясняющее, что
условие не выполняется), что добавляет грамматику сверх целевой конструкции.

**Прототип движка "конструкция + словарь по уровню" — сделан, механически
проверен** (без реального Gemini-ключа):
- [engine/cefr_wordlist.py](engine/cefr_wordlist.py) — загружает CEFR-J
  словарь, `words_for_level(level, pos, limit)` отдаёт слова нужной части
  речи на/ниже уровня ученика.
- [engine/constructions.py](engine/constructions.py) — реестр РУЧНЫХ
  оверрайдов, ключ — `lesson_id` (не отдельный "construction_id" — обобщили
  сразу под все 173 урока imlls, см. ниже). Оверрайд нужен только там, где у
  урока реальный дефект в собственных фразах (пока один — 139, "however"-
  довесок); остальные 172 урока не требуют записи в этом файле вообще.
- `engine/gemini.py::generate_lesson_construction_drill(lesson_id, topic,
  seed_phrases, user_level, native_lang, n)` — обобщённая версия (была
  `generate_construction_drill` с одной захардкоженной конструкцией, заменена
  этой функцией по прямому запросу Натальи "промпт для всех уроков"). Паттерн
  берётся НЕ из ручного описания, а автоматически — тема (`topic_en`) + 2-4
  реальных примера фразы урока, которые caller достаёт сам через
  `engine.loader` и передаёт (`topic`/`seed_phrases`), точно как уже устроен
  параметр `phrases` у `generate_practice_test` — gemini.py по-прежнему не
  трогает БД напрямую. Если для lesson_id есть запись в
  `engine.constructions` (дефект найден) — паттерн берётся из неё ВМЕСТО
  seed_phrases, чтобы баг урока не тиражировался в каждый сгенерированный
  дрилл.
- **Проверено на реальных данных** (imlls_database через `engine.loader`,
  `_model` подменён на фейковый — реального Gemini-ключа нет): урок 1
  ("Describing things") — паттерн корректно выведен из примеров ("a big
  dog"/"a big cat"...) + подставлен реальный пул слов уровня A2 из CEFR-J;
  урок 139 — override сработал, промпт использует чистое описание Second
  Conditional вместо фраз с "however". Сама генерация Gemini (естественность,
  реальное соблюдение ограничения по словарю) — не проверена вживую, нужен
  `GEMINI_API_KEY`.
- Английский-only (как и решили) — `cefr_wordlist` не имеет пула для uk/es/ko,
  эквивалентного открытого CEFR+POS-источника для них не нашлось; функция
  жёстко генерирует на английском независимо от target_lang ученика, пока
  это не расширено.

**Интеграция в grammar.py — только Phase 3 (Praktyka), Phase 2 не тронута.**
Разобрала архитектуру урока перед тем, как встраивать: Phase 2 ("Novyi
material", 8 шагов) целиком построена на `session.phrases()` — фиксированных
7-8 фразах урока, и озвучка для них кешируется на диске по хэшу
`(язык, текст)` ([engine/tts.py:30](engine/tts.py:30)), общий кеш на всех
пользователей. Если бы туда подставлялись динамически сгенерированные
предложения — кеш промахивался бы каждый раз, озвучка шла бы вживую на
каждой попытке каждого юзера. Phase 3 ("Praktyka"), наоборот, и так уже
живая генерация при каждом клике (`generate_practice_test`) — естественное
место, ничего нового по инфраструктуре/стоимости не добавляет.

Сделано в [grammar.py](grammar.py)::`phase3_practice` — добавлен пункт
`construction_drill` в существующий селектор `test_type`, показывается
только когда `module == "grammar"` и `target_lang == "English"` (пилот).
При выборе `_gemini.generate_lesson_construction_drill(lesson_id, topic,
session.phrases()-как-seed, level, native_lang)` вызывается вместо
`generate_practice_test`, а результат переупаковывается в тот же формат
`{question, answer, options}`, что и остальные test_type — как перевод
(native → target) — поэтому существующий рендер и цикл проверки
(`check_practice_answer`, сбор ошибок через `_collect_error`) не потребовали
ВООБЩЕ никаких изменений, только генерация другая. i18n-ключ
`construction_drill` добавлен в en/uk (es/ko используют английский фолбэк —
как и остальные ключи Phase 3, у них тоже нет своих переводов для этого
экрана).

**Проверено** (реальные данные imlls_database + фейковая модель вместо
Gemini, реального ключа нет): переупакованный `p3_test` для урока 139 —
корректная форма `{"instructions": ..., "items": [{"question": <укр.
предложение>, "answer": <англ. предложение>, "options": []}]}`, `py_compile`
проходит на всех изменённых файлах. **Не проверено вживую** — реальный клик
через Streamlit UI и реальная генерация Gemini, нужен `GEMINI_API_KEY` и
живой логин.

**Побочная находка — исправлена по просьбе Натальи** ("чини сейчас",
2026-08-21): `grammar.py::_lesson_level` раньше определяла CEFR по грубым
порогам lesson_id (≤30→A1, ≤70→A2, иначе B1, потолок B1 — B2 никогда не
возвращался), расходясь с настоящим источником уровня в этом же проекте —
`DIFFICULTY_TO_CEFR` (по полю `difficulty` из imlls_database, доходит до
B2), которым уже пользуется `scripts/seed_content_units.py` при заполнении
`content_units`. Например для урока 139 (реальная сложность 4 = B2)
`_lesson_level` возвращала "B1" — на уровень ниже, значит и словарный пул в
`construction_drill` подтягивался бы чуть проще, чем должен.

Исправлено в корне, а не заплаткой поверх:
- `DIFFICULTY_TO_CEFR` перенесён из scripts/seed_content_units.py в
  [engine/recommender.py](engine/recommender.py) (рядом с уже существующим
  `CEFR_RANK`) — единый источник истины; seed-скрипт теперь импортирует
  оттуда вместо своей копии, дальше эти две константы разойтись не смогут.
- `grammar.py::_lesson_level` теперь принимает `session` (не голый
  `lesson_id`) и берёт `difficulty` прямо из уже загруженных фраз урока
  (`session.phrases()[0]["difficulty"]`) — для Grammar эта колонка реальная
  (из imlls_database), маппится через `DIFFICULTY_TO_CEFR`. Для Vocabulary
  (`vocabulary_translated.xlsx` не тегирован по-уровнево на урок — это тот
  же C2-баг из аудита выше) колонки `difficulty` в загруженном df нет вообще
  — функция сама это видит (`.get("difficulty") is None`) и падает на старый
  lesson_id-эвристику как и раньше, ничего там не сломано.
- 4 места вызова обновлены на новую сигнатуру (`session` вместо
  `session.state.lesson_id` / `state.lesson_id`).
- **Проверено на реальных данных** (imlls_database, без БД — sqlalchemy нет
  в песочнице, поэтому логика проверена напрямую, не через
  `engine.recommender`'s импорт): grammar lesson 1→A1, 70→A2, 114→B1,
  139→B2, 173→B2 — границы совпадают с реальным `difficulty` по всему
  диапазону, не только на 139. `py_compile` проходит на всех правленых
  файлах (grammar.py, engine/recommender.py, scripts/seed_content_units.py).
  Не проверено вживую через Streamlit (не запускала реальный сервер в этой
  сессии).

## Фикс C2-тегов словаря — данными, не на глаз (2026-08-21, продолжение)

Наталья попросила почини это первым из списка перед Фазой E. Вместо ручной
разметки (как раньше делала "Basic" — по памяти о старом `VOCAB_STAGES`),
использовала уже принесённый CEFR-J словарь ([data/cefr_j/](data/cefr_j/)) —
данные, а не догадка.

**Метод**: для каждого из 5 листов (Verbs/Food/City/Hobbies/Restaurant) взяла
только уроки-СПИСКИ СЛОВ (не уроки-предложения — нечётные lesson_id у
Verbs/Food/City, где структура "слова → потом те же слова в предложениях";
Hobbies/Restaurant устроены иначе, там пары фраза+пример чередуются построчно
внутри одного урока, не по чётности lesson_id — метод чуть менее точен для
этих двух, но по факту не критично, см. ниже), токенизировала английский
текст, сопоставила каждое слово с реальным CEFR-J уровнем (минимальный
уровень, если слово встречается в словаре с разными частями речи), взяла
МЕДИАНУ по листу — тот же принцип "в пользу доступного нижнего предела", что
уже использовался для "Basic" в Фазе C.

**Результат** (реальное распределение уровней слов на листе, не только
медиана):
- Verbs: медиана A1, p75 A2 — распределение A1=448, A2=147, B1=130, B2=46,
  C1=5, C2=3 (779 сопоставленных токенов).
- Food: медиана A1, p75 A2 — A1=394, A2=157, B1=118, B2=52, C1=10, C2=2 (733).
- City: медиана A1, p75 A2 — A1=57, A2=20, B1=4, B2=3 (84).
- Hobbies: медиана A1, p75 A1 — A1=119, A2=14, B1=12, B2=5 (150).
- Restaurant: медиана A1, p75 A1 — A1=116, A2=7, B1=16, B2=2, C1=1 (142).

Все 5 листов реально доминированы бытовой A1/A2-лексикой — C2 был не просто
неточным, а полностью перевёрнутым (там где должен быть самый низкий уровень,
стоял самый высокий). [data/vocab_tags_template.csv](data/vocab_tags_template.csv)
обновлён: все 5 строк → `A1` (было `C2`), только эти 5 строк — остальные 24
не трогала.

**Известный, не исправленный сейчас нюанс** (тот же паттерн, что уже был
задокументирован для "Basic" в Фазе C, не новая проблема): Verbs (1160 строк)
и Food (1386 строк) — большие листы, реально охватывающие A1 до C1/C2
(видно по распределению выше), а схема тегирует целый лист ОДНИМ уровнем.
Теперь этот один уровень хотя бы в разумную сторону (A1, доминирующий тон
листа), а не в противоположную (C2) — но точность на уровне отдельного урока
внутри листа по-прежнему грубая. Более точная разбивка (тегировать не лист
целиком, а каждый урок отдельно — используя тот же метод, лист по листу)
осталась бы отдельной, более крупной доработкой, если понадобится позже.

**Не сделано**: сама БД не обновлена — этот CSV читает
`scripts/seed_content_units.py` (без флага) при записи в `content_units`, но
у меня нет `DATABASE_URL`. Наталье нужно перезапустить сид-скрипт против
реальной Supabase, чтобы фикс подействовал:
```bash
python scripts/seed_content_units.py
```
(безопасно перезапускать — `UPSERT`, как и раньше в Фазе C).

## Фикс краша на Python 3.9: `TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'` (2026-08-21, продолжение)

Наталья запустила приложение локально (`.venv`, Python 3.9) и получила краш
при импорте `grammar.py` → `engine/session.py` → `class SessionState` (поле
`unit_id: str | None = None`). Причина: аннотации вида `X | None` (PEP 604) в
теле класса/функции вычисляются немедленно при исполнении модуля, если нет
`from __future__ import annotations` — под Python 3.10+ `str | None` работает
(новый синтаксис union), под 3.9 `type.__or__` для этого не определён →
падает сразу при импорте, ещё до какого-либо реального использования кода.
Не связано с сегодняшними правками сессии — это предсуществующий баг,
подсвеченный только сейчас, потому что её `.venv` оказался на 3.9 (раньше,
видимо, работала на более новом Python локально).

**Масштаб оказался шире одного файла** — в проекте уже ЕСТЬ устоявшийся
паттерн (19 файлов используют `from __future__ import annotations` именно
чтобы `X | None` работал на старых Python), но ещё 6 файлов использовали тот
же синтаксис БЕЗ этого импорта:
[engine/session.py](engine/session.py) (сам краш),
[engine/tts.py](engine/tts.py), [engine/vocab_loader.py](engine/vocab_loader.py),
[reading_app.py](reading_app.py), [grammar.py](grammar.py) (6+ мест),
[generate_app_images.py](generate_app_images.py). Починила все 6 — добавила
`from __future__ import annotations` сразу после докстринга в каждом (тот же
паттерн, что уже используется в остальных 19 файлах, поведение кода не
меняет, только откладывает вычисление аннотаций).

**Проверено**: `py_compile` проходит на всех 6 файлов; полный AST-скан всего
репозитория (искала `X | Y` в аннотациях без `from __future__ import
annotations` рядом, за вычетом .venv/venv/сторонних пакетов) — 0 оставшихся
файлов с этим паттерном. **Не проверено вживую** — не запускала `streamlit
run app.py` под Python 3.9 сама (нет такого окружения в песочнице).

## Фикс краша `st.login()`: "400: 'NoneType' object does not support item assignment" (2026-08-21, продолжение)

После двух предыдущих фиксов (Python 3.9 union-синтаксис, недостающие пакеты)
Наталья дошла до экрана логина, нажала "Continue with Google" — экран стал
пустым, URL завис на `localhost:8501/auth/login?provider=<JWT>`, а сам
Streamlit показал `400: 'NoneType' object does not support item assignment`.

**Не в её коде — версийный баг в связке Streamlit↔Authlib.** Прочитала
исходники установленных пакетов (не гадала): `AuthLoginHandler.get()` в
streamlit ловит любое исключение из `client.authorize_redirect(...)` и
показывает его текст как HTTP 400 reason — значит сам `authorize_redirect`
падал. Проследила цепочку до
`streamlit/web/server/oidc_mixin.py::TornadoOAuth2App._save_authorize_data()`
— она намеренно вызывает `self.framework.set_state_data(session=None, state,
kwargs)`, полагаясь на то, что Authlib трогает `session` ТОЛЬКО если кеш не
настроен (у Streamlit кеш всегда настроен — `engine.auth_util.AuthCache`).
Но в `Authlib>=1.6.6` (`FrameworkIntegration.set_state_data`,
`authlib/integrations/base_client/framework_integration.py`) веткa `if
self.cache:` **всё равно** делает `session[key] = {...}` — а `session` там
`None` → `TypeError`. Подтверждено официальным issue
[streamlit/streamlit#13461](https://github.com/streamlit/streamlit/issues/13461)
— Authlib 1.6.5 работает, 1.6.6+ ломает. `requirements.txt` до сих пор был
`Authlib>=1.3.2` без верхней границы — свежая установка (я сама поставила
пакеты в предыдущем фиксе) взяла последнюю 1.6.12 и попала прямо в баг.

**Исправлено**: [requirements.txt](requirements.txt) — `Authlib==1.6.5`
(жёстко, не `>=`, с комментарием почему), переустановлено в её `.venv`.

**Проверено по-настоящему** (не логическим тестом): вызвала
`create_oauth_client("default")` + `client.authorize_redirect(...)` напрямую
в её `.venv` с её реальными secrets.toml — раньше падало с той же ошибкой,
что видела она; после пина Authlib==1.6.5 успешно вернуло настоящий URL
авторизации Google (с её `client_id`, верным `redirect_uri`, PKCE
`code_challenge`, `scope=openid email profile`). Сам клик в браузере и
прохождение до конца (редирект на Google → согласие → возврат в приложение)
— не проверено, это её реальный Google-аккаунт.

## Переосмысление Vocabulary: Word Bank → CEFR-J, разговорник — отдельный модуль (2026-08-21, продолжение)

Наталья посмотрела Vocabulary и предложила реструктуризацию: "Word Bank"
(Basic/Verbs/Food/City — 4795 из 5015 строк, те самые листы с алфавитной
бакетизацией и C2-багом из аудита выше) — заменить на список по CEFR-J
лицензии, это станет новым модулем **Vocabulary**. Остальные ~24 листа
(Greetings/Hobbies/Restaurant/...) — реально разговорник (коллокации +
примеры в контексте, не изолированные слова) — выделить в отдельный новый
модуль **Разговорник**, оставив контент как есть.

**Решено в чате**: весь список CEFR-J (не урезать — 9779 слов A1..C2, это
больше нынешнего Word Bank, но так и задумано). Генерация примеров —
**только на английском** (без батч-перевода на uk/es/ko заранее —
существующий `engine.gemini.translate_phrase()` уже переводит на лету и
кеширует, пересобирать 9779 слов на 3 языка заранее не нужно). Пример-
предложение на каждое слово строится **из пула грамматики соответствующего
уровня** — не общая генерация, а конкретная грамматическая конструкция
(тема урока imlls_database того же CEFR-уровня), чтобы отрабатывать слово на
уже знакомой грамматике, а не вводить новый паттерн предложения заодно с
новым словом. Это тот же принцип "конструкция + словарь по уровню", что и
`generate_lesson_construction_drill`, только развёрнутый в обратную сторону
(там уровень фиксирует конструкцию и подбирает словарь; здесь слово
фиксировано, а конструкция подбирается по его уровню).

**Сделано:**
- `engine/cefr_wordlist.py::all_entries(level=None)` — новая функция, весь
  список CEFR-J как `[{"headword", "pos", "level"}, ...]`, дедуплицирован по
  (headword, pos) с минимальным уровнем при конфликте. Проверено: 9779
  записей (A1=1164, A2=1411, B1=2446, B2=2778, C1=1046, C2=934).
- [scripts/generate_vocab_from_cefrj.py](scripts/generate_vocab_from_cefrj.py)
  — новый одноразовый (возобновляемый) генератор:
  `build_grammar_pool()` группирует грамматические уроки imlls_database по
  CEFR-уровню (тот же `DIFFICULTY_TO_CEFR`, что и в seed_content_units.py —
  единый источник истины не нарушен); `pick_construction()` берёт случайный
  урок нужного уровня; **известный, осознанный пробел**: грамматика
  физически не существует выше B2 (см. "9 пробелов" выше) — C1/C2-слова
  падают на фолбэк B2-пула (`_FALLBACK_LEVEL`), это временно, до тех пор
  пока пробелы 1-9 не дописаны; после — перегенерировать эти строки с
  `--overwrite`. Результат — `data/vocabulary_cefrj.csv`
  (headword, pos, level, sentence, source_grammar_lesson_id).
- Скрипт резюмируем: `load_done()` читает уже сгенерированные пары
  (headword, pos) из существующего CSV, пропускает их при повторном
  запуске без `--overwrite` — прерванный прогон можно просто перезапустить,
  не оплачивая уже сделанные слова заново.
- `--limit`/`--level` — для тестового прогона на малой выборке перед полным
  запуском (9779 живых вызовов Gemini — реальная, хоть и одноразовая,
  стоимость, нужен её `GEMINI_API_KEY`).

**Проверено** (реальные данные imlls_database + CEFR-J, `_model` подменён на
фейковый — реального ключа нет): пул грамматики по уровням — A1=69 уроков,
A2=44, B1=25, B2=35 (совпадает с difficulty-распределением из аудита выше);
промпты для A1/B1/C1(фолбэк-B2)/C2(фолбэк-B2) собираются корректно с
реальными темами и примерами из imlls; полный прогон на 5 словах (fake-
модель) записал корректный CSV; повторный прогон без `--overwrite`
подтверждённо пропускает уже готовые (headword, pos) — протестировано
отдельно на управляемом наборе (3 заранее записанные пары, все 3 корректно
определены как "уже сделано"). **Не проверено**: реальная генерация Gemini
(качество предложений, соблюдение уровня) — нужен реальный ключ, это её
следующий шаг (тестовый прогон `--limit 20`, посмотреть на качество, потом
полный).

## Живая проверка тестового прогона (20 слов, реальный Gemini) + доработка промпта (2026-08-21, продолжение)

Наталья прогнала `--limit 20 --level A1` с реальным ключом, прислала все 20
результатов. Разобрала построчно — нашла 3 системные, повторяющиеся
проблемы (не разовые вкусовщины):

1. **Целевое слово иногда вообще отсутствует в предложении** — "those" →
   "Are there some apples in the box?" (слова "those" нет).
2. **Модель иногда выдаёт вопрос+ответ вместо одного предложения** (4 из
   20 — систематично): "green" → "Where does he buy fruit? He buys fruit in
   a green shop.", "officer" → "What is it? It's an officer.", "should" →
   "This is a boy. The boy should go to school.", "boyfriend" → "Who has a
   boyfriend? - Maria." Причина видна по данным: часть тем imlls сами
   состоят из вопрос-ответных пар ("Do you like coffee? - Yes, I do."),
   которые попадают в промпт как "образец паттерна" — модель копирует их
   двухчастную форму, несмотря на явную инструкцию "верни только одно
   предложение".
3. **Смысловые нестыковки от механического натягивания слова на чужую
   конструкцию**: "under the **blue** tree" (цвет утёк из темы "спрашиваем
   про цвет" и приклеился к случайному существительному), "**green** shop"
   (та же тема про цвет, но целевое слово САМО цвет — двойная путаница),
   "Has she got a good **self**?" (self вне контекста myself/self-esteem
   неестественно как отдельное существительное), "**Be shy!**"
   (повелительное наклонение для черты характера — прагматически странно).

**Исправлено в промпте** (`build_prompt()` в
[scripts/generate_vocab_from_cefrj.py](scripts/generate_vocab_from_cefrj.py)):
явное правило "ровно одно предложение — если образцы вопросы, верни только
вопрос, без ответа"; явное правило "если паттерн не ложится естественно на
слово — адаптируй его, естественность важнее жёсткого соответствия
паттерну" (смягчает проблему 3, не решает полностью — некоторые слова типа
"self" в принципе плохо подходят для изолированной генерации, это
приемлемый остаточный шум, не системная ошибка).

**Добавлена автоматическая проверка** (решает проблему 1): новая
`contains_word()` — регулярка на границы слова, регистронезависимая, ловит
словоформы (learn/learns/learned). `generate_sentence()` теперь проверяет
результат и **делает одну повторную попытку** с усиленным промптом
("в прошлый раз слова не было — обязательно включи его"), если слово
отсутствует; если и повтор не помог — строка всё равно записывается (не
теряем данные), но в консоль печатается `WARNING`, чтобы видеть реальный
процент проблемных слов после полного прогона, а не гадать.

**Проверено** (фейковая модель, реального ключа нет): `contains_word` верно
отличает "those" в "Are there some apples..." (False) от "Those apples are
red." (True), корректно матчит словоформы (learn/learned). Retry-механизм
проверен явно на управляемом сценарии: первый (фейковый) ответ без слова →
автоматически второй вызов с усиленным промптом → второй ответ содержит
слово → возвращается он, без warning. Отдельно проверен противоположный
случай — если и повтор не содержит слово, warning печатается, строка всё
равно возвращается (не теряется).

**Второй тестовый прогон (20 слов, реальный Gemini, обновлённый промпт) —
разобрала, нашла и починила один реальный баг, отменила один свой прошлый
вывод:**

- **Баг в `contains_word()`, не в генерации**: 3 из 4 warning'ов ("Mr./Mr",
  "airplane/aeroplane", "p.m./P.M./pm/PM" — всё та же особенность CEFR-J,
  headword'ы с несколькими написаниями через "/", как уже видели в
  "a.m./A.M./am/AM") оказались ложными — модель сгенерировала верно ("He is
  Mr. Smith.", "This is an airplane.", "She calls me at 8 p.m."), просто
  проверка искала весь составной "mr./mr" одной строкой, которая, конечно,
  никогда не встретится в обычном предложении. **Исправлено**:
  `contains_word()` теперь разбивает headword по "/" и проверяет каждый
  вариант написания отдельно. Проверено на всех реальных случаях из её
  прогона плюс исходном тесте (apples/those) — все 6 совпадают с ожиданием.
- **Отменяю свой прошлый вывод про "две части = плохо"**: в этом прогоне
  двухчастные ответы ("lot" → "Who is it? - It's a lot of people."; "sing"
  → "That's a bird. The bird can sing."; "yellow" → "Where does the sun
  shine? It shines on a yellow car.") при повторном разборе оказались
  вполне естественными способами показать слово в контексте — не дефект
  формата, а нормальный разговорный образец. Была слишком строга в прошлой
  оценке.
- **Единственная реальная шероховатость** — "finish" (noun) → "This is the
  end. The end is a finish." — неестественно, но похоже на разовую
  особенность конкретного слова (существительное "finish" — редкое
  словоупотребление, ближе к "self" из первого прогона), а не системная
  проблема промпта. Мелкие стилистические огрехи такого рода ("twenty" →
  "a twenty-year-old man" не полное предложение; "many" → "Who wants
  many?" слегка неестественно) — ожидаемый уровень шума для 9779
  автоматически сгенерированных предложений, не блокер.

**Вывод**: качество после фиксов достаточное для полного прогона — не
нашла больше системных проблем, только конкретный баг проверки (исправлен)
и штучные шероховатости отдельных слов (приемлемо для V1, можно точечно
почистить позже по мере использования).

## Дописаны 9 грамматических тем + очистка тестовых данных + код "Разговорника" (2026-08-21, продолжение)

**9 недостающих грамматических тем — написаны и проверены.** Наталья
попросила написать английскую часть 9 пробелов (см. список выше), если
уверена в качестве — уверена, это стандартные ESL-темы, есть в любом
учебнике (Murphy и т.п.). Добавлены уроки 174-182 в
`imlls_database_with_titles.xlsx` (English + украинский; остальные языки
намеренно пусты — при добавлении переводы уже существующих 9 языков не
трогала осознанно, добавлять их сейчас не просили):

174 Reported speech, 175 Relative clauses (who/which/that), 176 Third
conditional, 177 Modals of deduction, 178 Used to/would, 179 Wishes
(present/past regrets), 180 Phrasal verbs, 181 Causative (have/get
something done), 182 It+adj+to-infinitive. Уровни расставлены по данным
CEFR-J Grammar Profile (не на глаз): B1×5 (174/175/178/180/182), B2×3
(177/179/181), C1×1 (176, Third conditional — CEFR-J явно тегирует его C1).

**Для этого расширила `DIFFICULTY_TO_CEFR`** (engine/recommender.py) с
`{1..4}` до `{1..6}` (добавлены 5=C1, 6=C2) — раньше грамматика физически не
доходила выше B2 (см. пункт про побочную находку выше), теперь появился
первый настоящий C1-урок.

**Реальный баг, найденный и исправленный по дороге**: в первой версии
скрипта добавления перепутала колонки листа "phrases" — взяла `phrase_id`
из первой колонки (а это `lesson_id`, `phrase_id` — вторая). Из-за этого
новые `phrase_id` (174-245) задублировали бы уже существующие (phrase_id в
этом листе глобально уникален по всей книге, 1-1382, не сбрасывается по
уроку — проверила на бэкапе). Заметила до сохранения необратимых
последствий, восстановила файл из резервной копии, переделала правильно.
**Проверено дважды**: (1) после английской версии — 182 урока, 1452 фразы,
все ID уникальны, старые уроки 1 и 173 не задеты; (2) после добавления
украинского — `engine.loader.load_phrases()` (тем же кодом, что и
приложение) реально показывает все 9 уроков с 8 фразами и корректными
en/uk парами, старые данные по-прежнему не задеты.

**Очистка тестовых данных БД** (по прямой просьбе Натальи — "там только мои
тестовые"): проверила перед удалением (только чтение) — `mastery` (243),
`srs_state` (2), `lesson_pointer` (4), `gamification` (1) содержали только 2
user_id (`nataliiamaslovaneo@gmail.com` и старый dev-фикстур `student1`) —
подтвердила, что это действительно только её данные, прежде чем удалять.
Очистила все 4 таблицы (`DELETE FROM`) — теперь 0 строк в каждой.
`content_units` (999, каталог уроков) и `subscriptions` (1, похоже на
реальную Stripe-подписку) НЕ трогала — не были в списке, который она
подтвердила.

**Код для "Разговорника" — сделан и проверен, но НЕ полная интеграция:**
- [engine/vocab_loader.py](engine/vocab_loader.py) — `WORD_BANK_SHEETS =
  frozenset({"Basic", "Verbs", "Food", "City"})`; `load_vocab()` получил
  новый параметр `exclude_sheets` (по умолчанию None — полностью обратно
  совместимо с существующим вызовом для "vocab"). global lesson_id (gid)
  строится по ВСЕЙ книге независимо от `exclude_sheets` — **проверено
  напрямую**: для листа Hobbies gid'ы идентичны что в полной загрузке, что
  в отфильтрованной (`[474, 475, 476, 477, 478]` в обоих случаях) — значит
  существующие `unit_id` (`vocab:Hobbies:474`...) не сдвинутся при переходе
  на новый модуль.
- [grammar.py](grammar.py) — новая `_load_phrasebook()` (обёртка над
  `load_vocab(..., exclude_sheets=WORD_BANK_SHEETS)`) + новая ветка
  `_module_config("phrasebook")` (иконка 💬, label "Phrasebook"), рядом с
  существующими "vocab"/"grammar"/"custom".
- **Проверено**: полная загрузка = 4960 строк/29 тем, разговорник = 1473
  строк/**25** тем (не ~24, как оценивала раньше — 29 листов минус ровно 4
  Word Bank = 25) — Word Bank листы подтверждённо отсутствуют в
  отфильтрованном наборе.

**Не сделано (следующие шаги)**:
1. **app.py не знает про "phrasebook"** — там свой отдельный реестр модулей
   (словарь лаунчер-карточек ~строка 159, `elif module_key == "vocab":`
   диспетчеринг ~строка 280/634, хардкоженный список `["grammar", "vocab"]`
   в селекторе теста на уровень ~строка 522) — без правок там модуль
   технически существует в grammar.py, но никак не доступен из реального UI.
2. **DB-миграция** `content_units.module` CHECK-констрейнт в schema.sql
   сейчас `IN ('grammar', 'vocab', 'reading')` — нужно добавить
   `'phrasebook'`. Дальше — существующие 999 content_units-строк для 25
   "разговорник"-листов сейчас всё ещё помечены `module='vocab'` — нужно
   либо `UPDATE content_units SET module='phrasebook' WHERE source_lesson IN
   (...)` для этих 25 листов, либо просто пересидировать
   `seed_content_units.py` заново (`mastery`/`srs_state`/`lesson_pointer`
   всё равно только что очищены, реальной истории терять не может — самый
   безопасный момент это сделать).
3. Ещё не решено, куда денется CEFR-J-контент (`data/vocabulary_cefrj.csv`,
   генерация всё ещё идёт) — нужен отдельный загрузчик под его CSV-формат —
   переключать "vocab" на CEFR-J только после того, как генерация закончится
   и Наталья одобрит качество.

## Разговорник доведён до конца — миграция БД + app.py + фикс флакующей сети (2026-08-22)

Наталья подтвердила делать всё сразу (миграция схемы + app.py + пересидирование).

**Разрешение на очистку** — заодно попросила удалить тестовые данные в БД
("там только мои тестовые"). Перед удалением проверила (только чтение):
`mastery`(243)/`srs_state`(2)/`lesson_pointer`(4)/`gamification`(1) содержали
только 2 user_id — её реальный email и старый dev-фикстур `student1`.
Подтверждено — очистила все 4 таблицы. `content_units` (каталог) и
`subscriptions` (похоже на реальный Stripe) не трогала — не входили в её
подтверждённый список.

**Более глубокая находка при проектировании миграции**: `unit_id` в этом
проекте — не просто ключ, `engine.recommender.parse_unit_id()` читает его
ПЕРВЫЙ сегмент как источник истины о модуле (`"vocab:Food:12"` →
`module="vocab"`). Значит нельзя было просто перекрасить колонку
`content_units.module` на 'phrasebook', оставив `unit_id` с префиксом
"vocab:" — это создало бы рассинхрон между тем, что говорит колонка, и тем,
что говорит сам ID. Раз реальной истории пользователей уже не осталось
(только что очистили) — переключила `unit_id` разговорника на собственный
префикс `phrasebook:<sheet>:<gid>`, это чище и не требовало сложной миграции
существующих строк:
- [engine/recommender.py](engine/recommender.py) — `parse_unit_id()` и
  `unit_id_for()` получили ветку для `"phrasebook"`.
- [schema.sql](schema.sql) — `content_units.module` CHECK расширен до
  `('grammar', 'vocab', 'reading', 'phrasebook')`.
- **Применено к реальной БД напрямую** (есть доступ через `engine.db`,
  использовала его же для очистки тестовых данных) — не давала ей ручные
  SQL-команды, накатила `ALTER TABLE ... DROP/ADD CONSTRAINT` сама, проверила
  через `pg_constraint` — новое ограничение подтверждено на месте.

**scripts/seed_content_units.py::seed_vocab()** переписан — делит по тому же
`WORD_BANK_SHEETS`: Word Bank → `module="vocab"`, остальные 25 листов →
`module="phrasebook"`, у каждого свой префикс unit_id. `main()` теперь чистит
`content_units` перед пересидированием (без этого остались бы осиротевшие
старые `vocab:Hobbies:*` строки рядом с новыми `phrasebook:Hobbies:*` —
content_units это каталог, не история пользователя, чистить безопасно).

**Реальный, неожиданный баг найден при живом прогоне против Supabase**:
исходный `db.upsert()`-цикл (по одной строке — ~1700 юнитов) открывал НОВОЕ
соединение на каждую строку (`engine/db.py::execute()` делает `with
get_engine().begin()` на каждый вызов) — соединение стабильно рвалось где-то
посередине (упало на юните ~163, потом ~28 при повторе). Попробовала
батчинг одним `execute_many()` на все 1008 юнитов разом — тоже оборвалось
(значит дело не только в количестве подключений, а в нестабильности самой
сети до Supabase из этой песочницы). **Финальный фикс**: разбила на чанки
по 50 строк, до 5 попыток на чанк с нарастающей паузой, плюс второй проход
в конце для чанков, не прошедших с первого раза (upsert идемпотентен —
повтор безопасен). Задокументировано прямо в коде скрипта — если кто-то
снова столкнётся с этой же ошибкой, объяснение уже на месте, а не только в
истории чата.

**Обнаружен и исправлен пробел консистентности**: после того как
"phrasebook" заработал в БД, поняла, что `_module_config("vocab")` в
grammar.py всё ещё грузил ВСЕ 29 листов (не только Word Bank) — то есть
живое приложение и БД разошлись бы (клик на "Vocabulary" показал бы те же
25 тем, что и "Phrasebook", но строил бы для них `unit_id` с префиксом
"vocab:", которого в content_units для этих тем уже не существует).
Добавлен `include_sheets` параметр в `load_vocab()` (зеркально к
`exclude_sheets`) — `_module_config("vocab")` теперь грузит ТОЛЬКО
`WORD_BANK_SHEETS`, ровно то же разбиение, что и в БД.

**app.py** — добавлена карточка "Phrasebook" (💬) в `MODULES`, диспетчинг
`elif active == "phrasebook": grammar_app.main(module="phrasebook")`,
отдельные счётчики прогресса (`_count_vocab_lessons` теперь считает только
Word Bank, новый `_count_phrasebook_lessons` — остальное).

**Проверено сквозно, от конфига модуля до реальной строки в БД**:
- `_count_vocab_lessons` + `_count_phrasebook_lessons` = 350 + 203 = 553
  (совпадает со старым общим числом vocab-уроков — ничего не потеряно, не
  задвоено).
- Для "phrasebook": `_module_config("phrasebook")["load"]` реально вернул
  1473 строки/25 тем; построенный на лету `unit_id_for("phrasebook", 1,
  "Greetings, Basics & Courtesy")` → `"phrasebook:Greetings, Basics &
  Courtesy:1"` — **точно совпадает** со строкой, реально найденной в
  content_units через `db.fetch_one`.
- То же самое для "vocab": `_module_config("vocab")["load"]` вернул 3487
  строк, только 4 темы (`Basic/City/Food/Verbs`) — построенный `unit_id`
  (`vocab:Basic:28`) точно совпадает со строкой в content_units.
- `py_compile` проходит на всех изменённых файлах.

**Не проверено вживую через реальный Streamlit UI** — клик по карточке
"Phrasebook" в браузере, реальное прохождение урока, запись
mastery/srs_state для нового модуля — логика проверена напрямую через
Python, не через клики.

**Не сделано**: селектор теста на уровень на лаунчере (`app.py::
_render_placement_quiz`) всё ещё захардкожен на `["grammar", "vocab"]` —
можно добавить "phrasebook" туда же, не сделала, т.к. не просили и это не
блокирует основной функционал.

## Живая проверка через браузер + кнопка в сайдбаре (2026-08-22, продолжение)

Наталья реально открыла оба модуля в браузере (скриншоты), не только через
Python-проверку:

- **`?module=phrasebook` открытый напрямую по URL — работает**: рендерит
  волновую навигацию с реальными темами ("Greetings, B...", "Questions,
  D..."), картинка/прогресс на месте.
- **Но в левом сайдбаре кнопки "Phrasebook" не было** — там отдельный,
  захардкоженный список `_SIDEBAR_MODULES` в `grammar.py` (нашла через
  агента, не гадала), независимый от `app.py::MODULES`, который я правила
  раньше. **Исправлено** — добавлена строка `("phrasebook", "💬",
  "Phrasebook")` в `grammar.py:2599` (между "vocab" и "reading" по порядку
  в меню). `py_compile` проходит.
- **Замечено, зафиксировано на потом (осознанно не чиню сейчас — так
  попросила Наталья)**: `?module=vocab` всё ещё показывает выпадающий
  список "Category" со ВСЕМИ прежними категориями (Communication, Word
  Bank, Situations, People, Home & Routine, Leisure...), а не только Word
  Bank — хотя сам загрузчик (`_load_word_bank`) уже отдаёт только 4 листа
  Word Bank, подтверждено напрямую в Python. Значит выпадающий список
  "Category" в пикере vocab строится из ОТДЕЛЬНОГО механизма группировки
  тем (не из `cfg["load"]()`'s результата напрямую) — какого именно, не
  разбирала, раз чинить не просили. **Идея Натальи**: разделить на два
  физических файла, когда CEFR-J-версия Vocabulary будет готова — тогда
  "Vocabulary" естественно перестанет видеть Phrasebook-категории (у него
  будет свой собственный источник данных), а не потребует патчить пикер
  сейчас, до того как понятно, как будет выглядеть финальная версия.
  Отложено до этого момента.

### Фаза E — PWA-упаковка (веб, без сторов)
- Манифест + service worker, чтобы приложение можно было установить с сайта.
  Публикацию в сторы не делать на этом этапе (см. пункт 6 выше).

### Фаза F — Публикация в сторах (только после подтверждения монетизации/retention)
- Google Play: Bubblewrap/TWA, таргет API 36 (см. фактчекинг выше).
- App Store: Capacitor + нативные доработки (push-уведомления, офлайн-прогресс) —
  без них высокий риск отклонения по Guideline 4.2.

## Vocabulary CEFR-J расширен на все языки + фикс "Category" дропдауна (2026-08-22, продолжение)

Наталья предложила: не делать отдельный CEFR-размеченный словник под каждый
target_lang — переводить тот же английский CEFR-J список (headword +
example sentence) на лету под любой target_lang, раз одни и те же слова
покрывают похожие ситуации в любом языке. Реализовано:

- **`engine/cefr_j_vocab_loader.py::load_cefrj_vocab()`** — теперь всегда
  хранит канонiчное английское предложение в новой колонке `source_en`;
  `native`/`target` заполняются напрямую только когда соответствующий язык
  — English, иначе остаются пустыми под ленивый перевод.
- **`engine/session.py`** — `_fill_missing_native()` обобщена в
  `_fill_missing_translations()`: тем же `gemini.translate_phrase()` (кеш
  в Supabase `phrase_translations`, оплачивается один раз за всю историю
  проекта, не за студента) теперь лениво заполняется и `native`, и
  `target` — переводится только ~10 фраз реально открытого урока.
- **`grammar.py::_load_vocabulary()`** — убран гейт `target_lang ==
  "English"`; CEFR-J теперь единственный источник Vocabulary для всех 14
  языков, старый `_load_word_bank()` удалён как мёртвый код.
- **`engine/recommender.py`** — `vocab_topic_for_lesson()` и
  `_candidates()` обновлены: рекомендатор тоже считает CEFR-J
  языконезависимым (а не только для English) и безусловно исключает Word
  Bank vocab-юниты из рекомендаций.
- **`scripts/seed_content_units.py`** — `seed_cefrj_vocab()` теперь ставит
  `source_lesson='cefrj'` вместо `'en'`, чтобы SQL-фильтр рекомендатора
  считал эти юниты доступными для любого target_lang. **Пересеяно в live
  Supabase** (`.venv/bin/python3 scripts/seed_content_units.py`, по
  явному запросу Натальи, так как это боевые данные): 1989 units
  (grammar=182, vocab=1331, phrasebook=203, reading=273), подтверждено
  прямым SQL-запросом — 981 vocab-юнит с `source_lesson='cefrj'`, 0
  оставшихся `source_lesson='en'`.

**Живая проверка (Наталья, пара Russian→Spanish) нашла 2 реальных бага**,
оставшихся от старого English-only гейта в местах, которые я не
затронула первым проходом:

1. `grammar.py` рендер-путь (~line 1327): при `module=="vocab" and target
   != "English"` код вообще пропускал загрузку `df` (ленивый Word-Bank-only
   путь), оставляя `df = None` → `AttributeError: 'NoneType' object has no
   attribute 'sort_values'` при построении превью уроков. Это и есть
   старая заметка выше ("Category" дропдаун показывал старые Word Bank
   категории вместо CEFR-уровней) — причина найдена: `render_setup()` в
   двух местах (~1428, ~1446) всё ещё проверяла `target == "English"`,
   отправляя любой другой язык в старый иерархический Category→Topic
   пикер (`_render_vocab_nav`, теперь удалён как мёртвый код) вместо
   плоской CEFR-навигации.
2. `app.py` плейсмент-квиз (`pq_module_used == "vocab"`) полагается на
   готовые (не через Gemini) пары native+target в `df_all` — раньше это
   работало для uk/es/ko через Word Bank, но `df_all` для vocab теперь
   всегда CEFR-J, где обе колонки одновременно заполнены только если
   native_lang И target_lang — оба English (никогда не бывает на
   практике). Реальный регресс, не просто edge case: раньше плейсмент-квиз
   работал для uk/es/ko, теперь — ни для одного языка. Починить "по-
   настоящему" нельзя без нарушения "квиз без Gemini для бесплатных
   пользователей" — вместо этого расширила предупреждение ("Vocabulary
   quiz isn't available yet") на все языки, а не только English, честно
   отражая новое состояние.

Попутно почищены ещё 2 места с тем же мёртвым `target != "English"`
гейтом, не ломавшие ничего (просто мёртвый/вводящий в заблуждение код):
`app.py::_count_vocab_lessons` (неверный счётчик уроков на главном меню)
и `engine/picker.py::_start_grammar_lesson` (мёртвый kwarg `topic=`,
молча игнорировался `_load_vocabulary`'s `**kwargs`).

После фиксов Наталья подтвердила в браузере ("works") — Vocabulary для
Russian→Spanish показывает плоский список уроков A1..C2 с превью по
headword'ам, как и для English.

## Phrasebook progress тихо не трекался + module focus для practice (2026-08-22, продолжение)

Наталья спросила, как генерируются Warmup/Practice/Speaking для
Vocabulary и Phrasebook. Разобрала — обе фазы делят один и тот же код
(`phase1_warmup`/`phase3_practice`/`phase4_expression`, `grammar.py`), и
нашла в процессе:

1. **`_MODULE_FOCUS`** (`engine/gemini.py`) — словарь фокуса
   practice-теста имел ключі лише `"grammar"`/`"vocab"`; для phrasebook
   `_MODULE_FOCUS.get(module, "")` повертав порожній рядок — practice-тест
   для Phrasebook генерувався без жодного фокус-фреймінгу. Додала ключ
   `"phrasebook"` (фокус на фразах/колокаціях, не граматиці/окремих словах).
2. **Реальний, значно серйозніший баг**: `_lesson_level()` для Phrasebook
   завжди падав на грубий lesson_id-фолбек (максимум B1), хоча
   `vocab_tags_template.csv` вже містить реальні CEFR-теги для всіх 203
   phrasebook-юнітів у `content_units` (перевірено напряму в БД). Причина
   виявилась глибшою: `vocab_topic_for_lesson()` викликався лише коли
   `lang_suffix == "vocab"` у ВСІХ 4 місцях запуску уроку (клік по
   картці, кнопка "Next lesson", deep-link по URL, сайдбар "jump to
   lesson") — для Phrasebook `_topic` завжди лишався `None`, тому
   `unit_id_for("phrasebook", ...)` повертав `None`, і **прогрес
   Phrasebook НІКОЛИ не писався в mastery/SRS** з моменту виділення
   модуля (2026-08-21) — тихий, непомітний баг, не пов'язаний з
   сьогоднішньою роботою над CEFR-J.

   Попутно знайшла ще одну регресію від сьогоднішнього переходу
   Vocabulary на CEFR-J: сайдбарна кнопка "jump to lesson" (`grammar.py`,
   ~2872) для vocab викликала СТАРИЙ `vocab_loader.get_topic_for_lesson()`
   напряму по `vocabulary_translated.xlsx` — а CEFR-J lesson_id (100000+)
   в тому файлі просто не існує, тому й ця кнопка для Vocabulary теж
   стала тихо нетрекованою сьогодні.

   **Фікс**: `engine.recommender.vocab_topic_for_lesson()` тепер спочатку
   пробує CEFR-J lookup, а якщо не знайдено — падає на
   `vocab_loader.get_topic_for_lesson()` (той самий global index, що й
   Word Bank, і Phrasebook, і pre-CEFR-J vocab lesson_id — усі три
   покриті одним резолвером). Усі 4 місця запуску уроку тепер викликають
   цей резолвер і для `"vocab"`, і для `"phrasebook"`.
   `_lesson_level()` (`grammar.py`) тепер, перш ніж падати на грубий
   lesson_id-фолбек, шукає `level` у `content_units` за `unit_id`.

   Перевірено на реальних даних (не в браузері — прямий Python +
   пряме SQL до Supabase): `vocab_topic_for_lesson` коректно резолвить
   і Word Bank sheet ("Basic"), і Phrasebook sheet ("Greetings, Basics &
   Courtesy"), і CEFR-J рівень ("A1") для відповідних lesson_id;
   згенерований `unit_id` для phrasebook-уроку 1 точно збігається з
   реальним рядком у `content_units` (`level='A1'`); для юніту 434 з
   реальним тегом `level='C1'` `_lesson_level()` тепер повертає `C1`
   замість жорсткого B1-стелі; фолбек на неіснуючий/непротегований
   `unit_id` коректно повертається до старої lesson_id-евристики, нічого
   не ламає. **Не перевірено вживу в браузері** — сама механіка
   record_result/mastery для Phrasebook з реальним студентом; варто
   пройти хоча б один Phrasebook-урок і перевірити, що mastery/SRS рядки
   з'явились у БД (той самий live-тест, що робили для Фази C).

## i18n працював лише для en/uk (частково es/ko) — фікс для всіх 14 мов (2026-08-22, продовження)

Наталья повідомила: "internationalization is not working for all languages".
Перевірила `engine/i18n.py` — підтвердилось, і гірше, ніж просто прогалини:

- `LANG_TO_CODE` мав записи лише для 4 мов (English/Ukrainian/Spanish/Korean)
  із 14 у `LANGUAGES`. Для решти 10 (French, German, Japanese, Chinese,
  Portuguese, Italian, Polish, Russian, Catalan, Dutch) `_code()` тихо
  повертав `"en"` — **весь UI 8-крокового флоу (кнопки, назви фаз,
  підказки) рендерився англійською**, без жодного індикатора, що це не
  переклад.
- Навіть Spanish/Korean не були повними — мали лише ~13 із 83 ключів
  (step_label/required/try_first/main_menu + titles/hints по 8 кроків).
  Решта ~55 ключів (warmup_strategy, practice_title, speaking_title,
  sos_label...) теж падали на англійський фолбек через `get()`'s
  по-ключовий fallback.

**Рішення (свідомо інше, ніж для CEFR-J Vocabulary)**: там ліниво
перекладаємо на льоту, бо 9779 слів × N мов — дорого генерувати наперед.
Тут — навпаки: фіксований, маленький набір (83 ключі), який рендериться
на КОЖНІЙ сторінці — ліниво перекладати означало б, що перше відкриття
будь-якої нової мови гальмує на десятках живих Gemini-викликів ще ДО
першого рендеру. Тому — одноразовий batch-скрипт, як
`generate_vocab_from_cefrj.py`, а не runtime-переклад.

**Зроблено:**
- [scripts/generate_i18n_strings.py](scripts/generate_i18n_strings.py) —
  одноразовий (резюмований) генератор: бере `STRINGS["en"]` як джерело,
  для кожної мови з `engine.loader.LANG_COLUMNS` (окрім English/Ukrainian
  — ці лишаються hand-written) генерує ТІЛЬКИ відсутні ключі (тобто
  es/ko отримують лише свої ~55 бракуючих ключів, а не всі 83 заново) —
  зберігає в [data/i18n_generated.json](data/i18n_generated.json).
  Викликає `gemini._model(...).generate_content()` напряму (як і
  `generate_vocab_from_cefrj.py`), а не paid-gated `translate_phrase()` —
  це одноразовий адмінський скрипт власника, не запит юзера. Резюмованість
  — просто перевіряє, що вже є у вихідному JSON, без окремого
  `load_done()`.
- `engine/i18n.py::_load_generated()` — мерджить згенерований JSON у
  `STRINGS` при імпорті: створює бакет мови, якщо його не було (10 нових
  мов), або домержує лише відсутні ключі (es/ko) — hand-written значення
  ніколи не перезаписуються. `LANG_TO_CODE` розширено до всіх 14 мов
  (той самий список, що в `engine.loader.LANG_COLUMNS`).
- **Прогнано повністю**: 983 нові рядки, 0 помилок (перевірено — жодного
  `FAILED` у логу). Тестовий прогін на 5 рядках іспанською перевірений
  вручну до повного запуску (природний переклад, емодзі збережені —
  промпт явно просить не займати emoji/стрілки).

**Перевірено programmatically** (не в браузері): `i18n.get()` для всіх 14
мов повертає реальний переклад, а не сирий англійський текст; винятки, де
переклад ЗБІГАЄТЬСЯ з англійським (наприклад "Video"/"Text"/"Level" в
de/es/it/nl/ca) — перевірені вручну, це не баг, а законні
когнати/запозичення в цих мовах, не пропущені ключі. hand-written
uk/es (`step_label`) підтверджено незайманими після мержа.
**Не перевірено вживу в браузері** — чи справді рендериться, наприклад,
французький UI при виборі French як native_lang; варто відкрити застосунок
з такою мовою і глянути на 8-кроковий екран.

## YouTube-відео для всіх 14 мов + рішення про B2+ (2026-08-22, продовження)

Наталья принесла список каналів (дослідження з ChatGPT — самé воно чесно
писало, що частина хендлів це "guesses/search links", плюс містило
utm_source=chatgpt.com у посиланнях). Проект вже мав власне правило з
Фази A (жива перевірка через браузер, не вигадані лінки) — тому канали
самі МЕХАНІЧНО не копіювала, а використала як стартовий список і знайшла
+ перевірила РЕАЛЬНІ окремі відео (не просто канали — `data/youtube_links.csv`
матчиться по конкретному `url`, не по каналу) через живий пошук на youtube.com.

**Рішення Наталі, яке звужує обсяг і спрощує архітектуру**: один спільний
список відео на рівень (не per-lesson/per-topic — `topic` лишається
порожнім, як і в вже наявному English-рядку), і тільки для A1/A2/B1.
Починаючи з B2 — жодної курації: показувати повідомлення "на цьому рівні
дивись що завгодно — свій улюблений серіал/фільм цільовою мовою" замість
підібраних відео.

**Зроблено:**
- Перевірила й додала в [data/youtube_links.csv](data/youtube_links.csv)
  по 3 реальних відео (A1/A2/B1) для 12 мов, яких раніше не було
  (Ukrainian, Spanish, Korean, French, German, Japanese, Chinese,
  Portuguese, Italian, Polish, Russian, Catalan, Dutch) — кожне знайдене
  через живий пошук на youtube.com, перевірено реальний перегляд/канал
  через `window.location.href` після кліку (не вигадано). Пріоритет —
  канали з дослідження, коли вони справді існують і підходять за рівнем
  (Nicos Weg/DW для German, Easy French/Spanish/Polish/Catalan/Dutch,
  Comprehensible Korean/Japanese/Chinese, Russian With Max) — де вони не
  підходили чи не існували під тим хендлом — знаходила альтернативу тим
  самим пошуком, а не вигадувала.
- **English B2/C1/C2 рядки видалені** з CSV — вони більше недосяжні
  (дивись нижче), тримати мертві дані сенсу нема.
- [grammar.py::phase5_video()](grammar.py) — новий early-branch:
  `beyond_curated = CEFR_RANK[level] >= CEFR_RANK["B2"]` — якщо true,
  показує `i18n.get(native_lang, "video_beyond_curated")` і взагалі не
  викликає `youtube_links.get_videos()` (не просто порожній результат —
  функція не викликається). Новий i18n-ключ `video_beyond_curated`
  (hand-written en/uk, згенерований для решти 12 мов тим самим
  `generate_i18n_strings.py`, резюмованість підтвердила — перегенерував
  лише 1 новий ключ × 12 мов = 12 рядків, 0 помилок).

**Перевірено programmatically**: `get_videos()` коректно повертає нові
рядки для всіх 12 мов (жодних проблем з CSV-екрануванням, хоч тайтли
містять коми/лапки/юнікод); `phase5_video()`'s `beyond_curated`-гілка
підтверджена читанням коду — для B2+ `get_videos()` не викликається
взагалі, тільки `st.info` з новим повідомленням. **Не перевірено вживу в
браузері** — сам екран Phase 5 (Відео) для нової мови на рівні A1/A2/B1,
і повідомлення "дивись що завгодно" на рівні B2+.

## Phase 5: список каналів замість одного відео (2026-08-23, продовження)

Наталья вживу протестувала попередній результат (скріншот Vocabulary,
Spanish A1) — все спрацювало (мова, рівень, тема, підбір відео), але
помітила: одне й те саме відео повторюється на РІЗНИХ уроках одного рівня
(бо список — один на рівень, не per-lesson, як і задумувалось). Її нова
ідея: показувати список КАНАЛІВ на кожному уроці, не одне відео.

**Зроблено:**
- [data/youtube_channels.csv](data/youtube_channels.csv) — новий файл,
  language/channel_name/channel_url. Заповнений реальними каналами,
  перевіреними напряму (ручка каналу зчитана зі сторінки після переходу,
  не вгадана) — здебільшого ті самі канали, що вже стояли за 43
  курованими відео з попереднього кроку (перевірка була вже зроблена
  тоді), плюс кілька додаткових ручок, яких не було зафіксовано раніше
  (Mandarin Click, Brazilian Portuguese with Sofia, Easy Polish, Sergey
  Storyteller, Dutch Daily, Easy German) і 2 канали для English (BBC
  Learning English, Easy English), яких раніше взагалі не шукала.
  1-3 канали на мову, 14 мов, усі 33 записи — реальні перевірені хендли,
  жодного вигаданого.
- [engine/youtube_links.py::get_channels()](engine/youtube_links.py) —
  новий читач `youtube_channels.csv`. `get_videos()`/`youtube_links.csv`
  лишені як є (не видалені) — досі коректні, просто більше не викликаються
  з `phase5_video()`; можуть знадобитись пізніше, якщо захочеться
  повернути "рекомендоване відео" поруч зі списком каналів.
- [grammar.py::phase5_video()](grammar.py) — замість блоку з мініатюрою +
  одним/трьома відео тепер рендерить простий список посилань на канали
  (`- [Назва каналу](url)`). Прибрано підбір по темі (topic_en/topic_disp)
  — канали не прив'язані до теми уроку, тільки до мови+рівня (рівень уже
  фільтрується через той самий `beyond_curated`, окремого рівня для
  каналів не потрібно, бо один список каналів підходить під усі рівні
  A1-B1 цієї мови).
- Новий i18n-ключ `video_channels_intro` — hand-written en/uk, згенеровано
  для решти 12 мов тим самим скриптом (12 нових рядків, 0 помилок).

**Перевірено programmatically**: `get_channels()` повертає правильний
список для кожної з перевірених мов (Spanish=3, German=2, Catalan=1,
Ukrainian=3, English=2, Dutch=3); `i18n.get(..., "video_channels_intro")`
повертає реальний переклад для uk/fr.

Наталья вживу перевірила результат (скріншот Spanish A1) — список каналів
дійсно рендериться. Але помітила: 3 канали на мову — замало, хоче більше
вибору, і принесла файл `відео лінки.pages` (той самий ChatGPT-дослідження
зі старого чату, тепер оформлене в Pages з таблицями по 14 мовах).

## Розширення списку каналів + фікс плейсхолдерів з помилками (2026-08-23, продовження)

**Читання .pages-файлу**: `.pages` — це zip з proprietary IWA
(Snappy+Protobuf) форматом, таблиці напряму не парсяться. Pages.app вже
був відкритий з цим документом (`osascript -e 'tell application "Pages"
to name of every document'` підтвердив) — скористалась
`export theDoc to POSIX file ... as PDF` через AppleScript замість
реверс-інжинірингу IWA. Отримала 9-сторінковий PDF, прочитала напряму.

**Розширення каналів**: як і раніше, не довіряла хендлам з документу
механічно (там саме зізнання "some handles were guesses/search links") —
кожен новий канал перевірила живим пошуком на youtube.com (навігація +
`get_page_text`, значно швидше за курацію відео, бо не треба точний
відеоролик, тільки факт існування каналу + хендл з тексту сторінки).
[data/youtube_channels.csv](data/youtube_channels.csv) виріс з 33 до 52
каналів (2-5 на мову замість 1-3): English +2 (British Council, Learn
English With TV Series), Ukrainian +1 (Slow Ukrainian), Spanish +2
(Dreaming Spanish, Spanish Playground), Korean +2 (Talk To Me In Korean,
GO! Billy Korean), French +2 (Alice Ayel, Français avec Nelly), German +1
(Learn German with Anja), Japanese +2 (いろいろな日本語, JapanesePod101),
Chinese +1 (Grace Mandarin Chinese), Portuguese +1 (Practice Portuguese),
Italian +1 (Podcast Italiano), Polish +1 (Polski z Anią), Russian +1
(Real Russian Club), Catalan +1 (Català al Natural), Dutch +1 (Learn
Dutch with Kim). Жодних змін коду не знадобилось — `get_channels()` вже
повертав ВСІ рядки для мови, без ліміту.

**Перевірено programmatically**: `get_channels()` для всіх 14 мов, сума
52 канали (роздруковано по кожній мові окремо). **Не перевірено вживу в
браузері** — сам розширений список у Phase 5.

## Фікс: плейсхолдери Step 8 з навмисними помилками (2026-08-23, продовження)

Наталья, дивлячись на Step 8 ("Create Your Own Sentences"), помітила:
сірий текст-приклад у полі вводу містить граматичні ПОМИЛКИ ("Yo tiene un
perro" замість "tengo", "Ella son alta" замість "es") — і хвилюється, що
учні підсвідомо запам'ятають неправильну граматику, бо цей плейсхолдер
показується на кожному Step 8 кожного уроку.

`grammar.py:1529-1534` — знайдено `PLACEHOLDERS` з явним коментарем
"placeholder examples with typical errors" (en/es/ko, усі три навмисно
неправильні). Замінено на граматично правильні речення в усіх трьох
мовах — формат (одна фраза на рядок) лишається тим самим, ризик зник.
Перевірено grep по всьому репо — інших місць з тим самим патерном
("typical error", старий текст плейсхолдерів) не знайдено, це був
єдиний випадок.

## Фікс: on_lesson_complete() ніколи не викликався — streak завжди "0 days" (2026-08-23, продовження)

Наталья запитала, як зараз записується, що урок пройдено. Розібрала весь
шлях і знайшла реальний, значний баг попутно.

**Що реально працює зараз:**
- Resume-pointer (`lesson_pointer`, для recommender/"Coming up"/My Path) —
  пишеться автоматично в кінці Phase 2 (8-крокового блоку), через
  `_save_step_progress(sess, 99)` → `recommender.save_pointer()`
  (`grammar.py:2946-2947`) — **до** Practice/Speaking/Video, не в самому
  кінці 5-фазної подорожі.
- Mastery/SRS — оновлюється на кожній перевіреній відповіді через
  `LessonSession.score()` → `recommender.record_result()`
  (`engine/session.py:128-146`).
- XP за окремий крок — `on_step_complete()` (`grammar.py:2969`), спрацьовує
  після кожного з 8 кроків Phase 2.

**Знайдений баг**: `on_lesson_complete()` (`engine/gamification.py:562`) —
саме тут рахується денна серія (`_update_streak`), `lessons_completed` і
бонусний XP за весь урок — ніколи не викликався в живому флоу. Єдині два
місця, що його викликали (`render_complete()` `grammar.py:1108` і
`phase5_summary()` `grammar.py:2591`), самі ніде не викликаються з
`main()` — мертвий код, залишок з дизайну до того, як додали Phase
1/3/4/5. Живий шлях Phase 5 ("Назад в головне меню") просто чистив стан
без жодного виклику `on_lesson_complete()`. Це, найімовірніше, і є
причина "🔥 0 days" на кожному скріншоті за весь чат.

**Фікс**: `grammar.py`'s Phase 5 dispatch (`main()`, ~line 3004) тепер
викликає `on_lesson_complete()` при кліку "Назад в головне меню" — той
самий working-паттерн, що вже живий у `reading_app.py:1886-1901`
(підтверджено читанням коду, той виклик реально досяжний з `main()`).
Toast'и (бонус XP / рівень / бейджі / стрік) кладуться в
`_pending_toasts` **після** `_clear_all()` (інакше `_clear_all()` їх одразу
стирає, бо чистить увесь `session_state`) — покажуться на початку
наступного уроку через Phase 2's step loop, той самий "показ із
затримкою" патерн, що вже прийнятий у `reading_app.py` для
`_pending_r_toasts` (не новий компроміс, що я вигадала, а вже наявна
конвенція проєкту).

**Перевірено на реальних даних** (не мок): викликала
`on_lesson_complete()` напряму на тестовому `user_id` проти live
Supabase — `streak_current` 0→1, `lessons_completed` 0→1, видано бейдж
"🌱 First step". Тестовий рядок видалено з `gamification` після
перевірки (`DELETE FROM gamification WHERE user_id = '__test_completion_wiring__'`).
**Не перевірено вживу в браузері** — сам реальний прогін: пройти урок до
кінця (Phase 1→5), клікнути "Назад в головне меню", перевірити що
сайдбар більше не показує "0 days" після повторного проходження в інший
день (стрік по днях — потрібно 2 різні дні, щоб побачити реальний
приріст, а не просто перше "1 day").

**Не займала** (свідомо, поза межами питання): `render_complete()` і
`phase5_summary()` лишаються мертвим кодом — не видаляла, бо це
повноцінні "Lesson Complete!" екрани з банером/персонажем/метриками
(могли бути свідомо відкладені для майбутньої переробки UI, а не просто
забуті), а не тривіальні функції на кшталт `get_hint()`. Видалення —
окреме рішення, не питали.

Все три пункта из списка ниже закрыты:

- **`.streamlit/imlls/`** — вложенная копия верхнеуровневых файлов (`app.py`,
  `grammar.py`, `custom_app.py` и др., без `engine/`), закоммиченная в репо не
  симлинком, а обычными файлами. За время работы над фазами A-D разошлась с
  корневыми файлами (`grammar.py` в копии — 129 KB против 144 KB в корне) и
  нигде не использовалась (проверено grep по всему репо на `imlls/` — совпадений
  нет). Удалена: `git rm -r --cached .streamlit/imlls` + `rm -rf`.
- **`engine/gemini.py::get_hint()`** — подтверждено, что не вызывается ни из UI,
  ни из `engine/recommender.py` (фаза C применения не нашла). Удалена функция и
  упоминание в докстринге модуля.
- **`google-generativeai` deprecated** — при проверке оказалось, что миграция на
  `google-genai` уже сделана (см. `_ModelHandle` в `engine/gemini.py`, комментарий
  "Migrated 2026-08-22"), и `requirements.txt` уже указывает `google-genai>=1.0.0`.
  Этот пункт просто не был отмечен как закрытый в логе — фиксирую сейчас, самой
  миграции делать не потребовалось.

## Філологічний аудит + мовні шляхи граматики поза англійською (2026-08-23)

За запитом Наталії зроблено філологічний аудит `data/imlls_database_with_titles.xlsx`,
`data/vocabulary_cefrj.csv`, `data/i18n_generated.json`, `data/reading_lessons.xlsx` —
повний текст у [LINGUISTIC_AUDIT.md](LINGUISTIC_AUDIT.md). Головна структурна знахідка:
усі 182 граматичні уроки побудовані на АНГЛІЙСЬКИХ граматичних категоріях (артиклі,
gerund/infinitive, causative have/get, it+adj+to-infinitive), перекладених на 13 мов —
для мов без цих явищ (uk/ru/pl/ko/ja/zh не мають артиклів і т.д.) заявлена граматична
мета уроку порожня, а натомість повністю відсутні явища, центральні саме для цих мов
(вид дієслова в uk/ru/pl, ser/estar і subjunctivo в es/pt, часточки та рівні ввічливості
в ja/ko, відмінкова система в de, показники виду в zh).

**Знайдені й виправлені конкретні помилки в даних:**
- Рід іменника "собака" в українській — систематично чоловічий у всіх ~14 місцях
  (мало б бути жіночий за нормою СУМ, як і в російській колонці того ж файлу).
  Виправлено напряму в `data/imlls_database_with_titles.xlsx` (бекап:
  `data/imlls_database_with_titles.xlsx.bak_20260823`).
- Уроки 174–182 (додані 2026-08-21) мали фрази лише en/uk — для решти 12 мов
  `engine/loader.py`'s фільтр (обидві колонки native+target мають бути заповнені)
  тихо ховав ці 9 уроків для БУДЬ-ЯКОЇ пари мов поза English↔Ukrainian. Перекладено
  всі 72 фрази + 9 назв тем на es/fr/de/ja/zh/pt/it/pl/ru/ca/nl/ko (AI-переклад,
  не звірений носієм — той самий рівень довіри, що й `i18n_generated.json`).
- Побічна знахідка: `engine/loader.py::_TOPIC_COL_FOR_LANG` читав назву теми уроку
  рідною мовою лише для 4 із 14 мов (en/uk/es/ko), хоча реальні переклади тем
  (`topic_fr`, `topic_de`...) існували в файлі для всіх 14 — носії решти 10 мов
  завжди бачили англійську назву. Виправлено — тепер генерується з `LANG_COLUMNS`,
  працює для всіх 14 без потреби руками дописувати нові мови в майбутньому.
- Урок 139 ("Hypothetical situations"/Second Conditional) — кожна фраза була ДВОМА
  реченнями (умовне + "however, ..." пояснення), що тестувало граматику поза заявленою
  темою (відомо з попередньої сесії, не було виправлено). Обрізано до однієї цільової
  умовної фрази в усіх 14 мовах.
- `data/i18n_generated.json::construction_drill` — виправлено буквальний машинний
  переклад "fresh"→їстівна свіжість (fr "frais", de "frisch", ca "fresc", nl "vers",
  pl "świeże", ru "свежее") і "construction"→будівництво (ja "建設", ko "건축", zh
  "建设", nl "Bouw") у всіх 12 згенерованих мовах. Точковий фікс одного ключа, не
  повний QA-прохід по решті ~980 рядків.

**Нова фіча — "мовні шляхи" (Наталіїн запит: "збудувати шлях для кожної мови для
вивчення граматики"):** замість того щоб руками перекладати фіксовані англійські
речення на 13 мов (дорого й ризиковано — саме так з'явилась помилка з "собака" і
"fresh"/"construction" вище), для явищ, яких у imlls_database взагалі немає,
використано вже наявний у проєкті патерн "конструкція + рівнева лексика"
(`generate_lesson_construction_drill`, Фаза 2026-08-21) — генерація NATIVE-FIRST
живим Gemini-викликом, без перекладу.

- [engine/target_grammar_paths.py](engine/target_grammar_paths.py) — реєстр
  "мовних шляхів": короткий (6-7 пунктів), пріоритезований список ГОЛОВНИХ
  структурних прогалин на мову (та сама методологія, що й "9 підтверджених
  прогалин" для англійської, CLAUDE.md 2026-08-21), CEFR-рівень + опис для
  промпту. 13 мов (English не потребує — курикулум і так побудований з її
  граматики): uk/ru/pl — вид дієслова + дієслова руху + відмінки; es/pt — ser/
  estar + subjunctivo; fr/it/ca — subjunctif/congiuntiu + минулі часи; de —
  відмінкова система + trennbare Verben; nl — порядок слів + scheidbare
  werkwoorden; ja — は/が, て-form, кейго; ko — 은/는 проти 이/가, рівні
  ввічливості; zh — 了/过/着, 把-конструкція. AI-складений роадмап, не звірений
  носієм кожної мови — як і `i18n_generated.json`, розумний перший прохід, не
  остаточна істина.
- `engine/gemini.py::generate_target_grammar_drill()` — дзеркало
  `generate_lesson_construction_drill`, але без прив'язки до lesson_id/
  seed_phrases з imlls — назва+опис теми з реєстру самі є "seed". Завжди
  падає на просту інструкцію "проста, поширена лексика рівня X" (як і
  construction_drill для не-англійських target — cefr_wordlist англомовний).
- `grammar.py::phase3_practice` — новий `test_type == "target_grammar"`,
  видимий у селекторі лише коли для `target_lang` є записи в реєстрі (для
  English/мов без реєстру пункт просто не з'являється). Показує другий
  селектор (яку тему з роадмапу практикувати), результат переупаковується в
  той самий `{question, answer, options}`-формат перекладної вправи — існуючий
  рендер/чек-цикл не змінено. Як і `construction_drill`, це Phase 3 — ефемерна
  практика, НЕ пишеться в mastery/SRS (той самий, вже наявний прецедент).
- i18n: нові ключі `target_grammar` / `target_grammar_topic_label`, en/uk
  hand-written, решта 12 мов перекладено вручну (не через
  `scripts/generate_i18n_strings.py` — просто 2 короткі UI-мітки).

**Перевірено** (фейкова модель замість Gemini, реального ключа немає): повний
шлях `st.selectbox` → реєстр → `generate_target_grammar_drill.__wrapped__` →
JSON-парсинг для теми "Вид дієслова: доконаний і недоконаний" (uk, A2) — прогін
успішний, `py_compile` проходить на всіх змінених файлах. **Не перевірено вживу**
через реальний Streamlit UI й реальний Gemini — потрібен `GEMINI_API_KEY` і
живий логін, як і з рештою Gemini-фіч у цьому проєкті.

**Переклад назв тем роадмапу рідною мовою учня** — не робила (13×14
комбінацій, ризик той самий, що з "собака"/"fresh" вище). Замість цього
кожна тема отримала `gloss_en` — короткий англійський глос, показується
поруч із назвою мовою цілі в селекторі й над вправою (`[A2] Aspekt
czasownika... (Verbal aspect: perfective vs imperfective)`) — той самий
принцип, що вже є для `topic_en`-фолбеку в основному курикулумі.

## Мовні шляхи: з ефемерної Phase-3-вправи в реальний трекований контент (2026-08-23, продовження)

Наталья попросила: знайти цим темам місце в БД, згенерувати речення
цільовою мовою наперед (не живою генерацією на кожен клік), переклад на
рідну — Gemini на льоту. Зроблено наскрізно, на реальній Supabase + реальним
`GEMINI_API_KEY` (обидва були доступні в цій сесії — перевірено напряму
перед тим, як щось міняти на живій БД).

**Уточнення по дорозі**: Наталья спершу запропонувала додати ці речення
прямо в `imlls_database_with_titles.xlsx` — пояснила, чому це не підійде:
той файл вимагає змістовно ЕКВІВАЛЕНТНИЙ переклад у всіх 14 колонках
одночасно (`load_phrases()` ховає урок, якщо native/target не заповнені
обидва). Вид дієслова принципово неперекладний 1:1 ("Я прочитав книгу" і "Я
читав книгу" — обидва йдуть в англійську як "I read the book", нема чим
заповнити `en` так, щоб зберегти те, що саме вчимо). Натомість — той самий
патерн, що вже є для CEFR-J Vocabulary: один канонічний текст (тут — мовою
самої теми, не англійською), переклад на рідну ліниво й кешовано.

**Зроблено:**
- [scripts/generate_target_grammar_drills.py](scripts/generate_target_grammar_drills.py) —
  одноразовий (резюмований) генератор: для кожної з 75 тем реєстру — один
  виклик `generate_target_grammar_drill()`, зберігається лише `target`-текст
  (не `native` — рідна мова кожного реального читача своя, перекладати
  наперед нема сенсу). Прогнано повністю: **75/75 тем, 587 речень**,
  [data/target_grammar_drills.csv](data/target_grammar_drills.csv).
  Тестовий прогін на 3 темах перевірений вручну перед повним — якість добра:
  правильно показує рід/відмінки/вид у контексті (наприклад
  `uk_aspect_basic`: "Кожного ранку я готую сніданок" (недоконаний,
  звичка) проти "Вчора ввечері я приготував смачну вечерю" (доконаний,
  завершено) — саме та опозиція, яку мали ілюструвати).
- [engine/target_grammar_loader.py](engine/target_grammar_loader.py) —
  `load_topic_drills(target_lang, topic_key, native_lang, n=5)`: читає CSV,
  бере випадкові `n` речень, перекладає кожне на `native_lang` через уже
  наявний `gemini.translate_phrase(sentence, from_lang=<мова теми>,
  to_lang=native_lang)` — той самий кеш `phrase_translations`, жодного
  нового API. Free-тариф — фолбек на мономовний показ (як і
  `_fill_missing_translations` для CEFR-J).
- **`content_units` — новий модуль `target_grammar`.** `schema.sql` CHECK
  розширено; **застосовано напряму до реальної Supabase**
  (`ALTER TABLE ... DROP/ADD CONSTRAINT`, перевірено через `pg_constraint`).
  `unit_id = target_grammar:<lang_code>:<topic_key>`, `source_lesson =
  <lang_code>` — той самий механізм, що вже прив'язує **Reading** до однієї
  мови в `recommender._candidates()`, спрацював без жодної зміни в самому
  фільтрі. `engine/recommender.py::parse_unit_id()` — нова гілка;
  `target_grammar_unit_id()` — новий хелпер для побудови ключа.
  `ALL_MODULES` розширено.
- [scripts/seed_content_units.py](scripts/seed_content_units.py)::`seed_target_grammar()` —
  нова функція, читає `TARGET_GRAMMAR_PATHS` × реально згенеровані
  (lang, topic_key) з CSV (тема без згенерованих речень не сідиться —
  інакше content_units мав би рядок, для якого нема що показати).

  **Знайдений і виправлений реальний production-баг по дорозі**:
  `main()` робив безумовний `DELETE FROM content_units` перед кожним
  пересіюванням (додано 2026-08-21, коли каталог ще не мав залежних даних —
  на той момент справедливо). Запуск зараз впав з
  `IntegrityError: ForeignKeyViolation` — `srs_state.unit_id` реально
  посилається на `reading:es:1` (справжній прогрес Наталі, не тестові дані,
  FK без CASCADE). **Не обходила це каскадним видаленням** — прибрала сам
  `DELETE` назавжди (лишився чистий upsert), задокументувала в коді чому.
  Осиротілі рядки зі старих unit_id-схем (якщо колись знову перейменується
  префікс) залишаться неактивним сміттям у каталозі — прийнятний компроміс
  проти повторного ризику для реального прогресу студентів.
- **`grammar.py::phase3_practice`** — `target_grammar`-гілка тепер бере з
  пулу (`target_grammar_loader.load_topic_drills`) замість живої генерації
  на кожен клік; falls back на живий `generate_target_grammar_drill`, якщо
  тема ще не встигла згенеруватись (захист від майбутніх нових тем у
  реєстрі без пересидування). **Check-кнопка тепер пише в mastery/SRS**
  через `recommender.record_result()` — досі жоден Phase-3 тип тесту цього
  не робив (усе було ефемерним); лише для `target_grammar`, решта
  (multiple_choice/fill_in_blank/construction_drill...) лишається як була,
  бо в них уже є Phase 2 з реальним трекінгом.

**Перевірено наскрізно на реальній БД** (одноразовий тестовий юзер,
`__test_target_grammar_wiring__`, зачищений після): `content_units` —
рівно 75 рядків `target_grammar`, усі 13 мов присутні (english не
потрібен);
`recommender._candidates('Ukrainian', module='target_grammar')` повертає
рівно 7 uk-тем, `_candidates('German', ...)` — рівно 7 de-тем, перетин
множин — порожній (жодного витоку між мовами); `record_result()` тричі
(2 правильні + 1 неправильна) → mastery EMA 0.328125 (формула
alpha=0.25 з нуля — збігається), `srs_state` — `reps=0` (скинуто
неправильною відповіддю), `due_date` завтра; `get_next()` після цього
коректно відсунув щойно практиковану тему на останнє місце (score 0.346
проти 0.575 у непрактикованої `uk_gender_agreement`) — recommender реально
реагує на результат. `translate_phrase()` перевірено напряму (обхід
`@_require_paid` через `.__wrapped__`, як і раніше в проєкті для
тестування без логіну): "Кожного ранку я готую сніданок для родини." →
"Chaque matin, je prépare le petit-déjeuner pour la famille." — правильно
переніс недоконаний вид у теперішній час французької.

**Не перевірено вживу через реальний Streamlit UI** — сам клік по кнопках
у браузері з реальним логіном (мій прямий доступ — тестовий
Python-скрипт + реальна БД, не клік). Наталья вже бачила попередню версію
фічі вживу (скріншот, Spanish target, Practice); ця сесія змінила
внутрішній механізм (пул замість живої генерації щоразу + запис у
mastery/SRS), UI-рендер не чіпала — варто перевірити після цього, що
все ще рендериться так само.

## Закриття пунктів 1 і 4 перед Фазою E (2026-08-23, продовження)

**Пункт 1 (Taste Skill) — рішення прийнято: не використовувати.** Наталія
доробить дизайн-систему Mova (`static/mova/`) власними засобами, без
стороннього скіла. Знято з блокерів Фази E.

**Пункт 4 (патерни вправ із книг) — підтверджено кодом, вже реалізовано.**
Не було задокументовано окремим записом раніше (журнал відстав від коду),
хоча в самих коментарях коду вже є посилання на CLAUDE.md 2026-08-22/23:

- `engine/gemini.py::generate_practice_test()` —
  `_TEST_TYPE_GUIDANCE["sentence_transformation"]` (рядки 338–351):
  класична трансформація речення (active/passive, statement/question,
  reported speech...) з Голицинського/Murphy. У селекторі `test_type`
  доступна лише для `module == "grammar"` (`grammar.py:2299`) — свідомо,
  бо це граматичний, не лексичний патерн.
- `engine/gemini.py::generate_open_question()` (рядки 622–680) — Cambridge
  Vocabulary in Use "Over to you" стиль: одне відкрите особисте питання по
  темі уроку, використовується у Фазі 4 (Висловлювання).
- **Обидві функції мовонезалежні**: промпт будується з параметрів
  `target_lang`/`native_lang`, без прив'язки до англомовного словника чи
  захардкоджених речень. На відміну від `construction_drill`, який досі
  прив'язаний до англомовного `cefr_wordlist` і НЕ має такої
  універсальності — це окрема, вже відома межа, не частина пункту 4.
- Перевірено читанням коду (не вживу через реальний Gemini-виклик) — у
  `grammar.py:2295-2298` і виклику `generate_open_question` (`grammar.py:2487`)
  немає жодного гейту на мову чи модуль; коментар у коді прямо підтверджує
  мовонезалежність `sentence_transformation`.

Разом з пунктом 7 (відео для всіх мов, закрито 2026-08-22) усі 4 відкритих
пункти зі списку "Фічи/правки, обсуждённые перед Фазой E" тепер закриті.
Єдине, що лишається перед Фазою E — жива перевірка сьогоднішніх фіч
(`target_grammar_paths`, лінгвістичні фікси) через реальний акаунт (див.
розділ вище).

## Ідеї на бэклог, обговорені з Наталією (2026-08-23) — не вирішено, не зроблено

**Ідея А: довільні уточнюючі питання по темі для платних юзерів, ліміт
~5/день, відповідь через Gemini.** Не плутати з наявним `chat_with_tutor`
(`engine/gemini.py:690`) — той навмисно відповідає ЛИШЕ target_lang, без
пояснень, для розмовної практики у Фазі 4. Ідея Наталії — інший режим:
пояснення "чому саме так" рідною мовою (чи двомовно), прив'язане до
конкретної теми уроку. Відкриті питання для наступної сесії: чи робити
окрему функцію в `engine/gemini.py`, чи розширити `chat_with_tutor` другим
системним промптом/режимом; де рахувати ліміт 5/день (найпростіше —
невелика таблиця-лічильник у вже наявній Postgres-БД, той самий патерн, що
`subscriptions`/`gamification`, скидається по UTC-даті); де в UI розмістити
(під час Фази 2 "Новий матеріал" чи Фази 6 розбору помилок — там і
виникають "чому" питання найчастіше).

**Ідея Б: у Фазі 5 (Відео), особливо на B2+ де курації нема ("дивись що
завгодно"), — дати учню готовий промпт для пошуку відео під його рівень і
інтереси через AI, з кнопкою копіювання (учень сам вставляє в
ChatGPT/Gemini/тощо).** На відміну від ідеї А — **нуль додаткових
Gemini-викликів з боку застосунку**, просто підстановка рівня/target_lang
у шаблон тексту (`st.code(...)` у Streamlit вже має вбудовану іконку
копіювання, окремий JS не потрібен). Природно закриває наявну прогалину:
зараз повідомлення `video_beyond_curated` для B2+ (`grammar.py::phase5_video()`)
чисто інформативне ("дивись що завгодно"), без жодної дії для учня.

Обидві ідеї — новий скоуп, не з початкового списку 7 пунктів; не
блокують Фазу E, якщо Наталія не вирішить інакше. Логічне місце — після
Фази E або як частина Фази E, якщо ідея Б встигне (дешева й без
Gemini-залежності).

## Ідея Б реалізована: копіювальний AI-промпт у Фазі 5 (2026-08-23, продовження)

Наталія підтвердила — реалізувала ідею Б з попереднього розділу.

- [grammar.py::_video_ai_search_prompt()](grammar.py) — новий хелпер,
  будує англомовний промпт-шаблон (target_lang/native_lang/level уже є
  людинозрозумілими англійськими назвами по всьому проєкту — та сама
  конвенція, що й у `chat_with_tutor`/`generate_open_question`, перекладу
  не треба). Промпт явно просить AI відповідати `native_lang` і **прямо
  забороняє вигадувати URL** — той самий принцип "жива перевірка, не
  вигадані лінки", який проєкт уже застосовує до власної YouTube-курації
  (Фаза A) — тут він захищає студента від чужих галюцинацій, не наших.
- [grammar.py::phase5_video()](grammar.py) — новий `st.expander`
  (`video_ai_search_title`), розгорнутий за замовчуванням саме коли
  `beyond_curated` (B2+, раніше там було тільки інформативне
  "дивись що завгодно" без жодної дії) і згорнутий, але доступний на
  A1–B1 як доповнення до списку каналів. Опційне поле `st.text_input` для
  інтересів студента підставляється в промпт; порожнє — лишає
  плейсхолдер-приклад. `st.code(..., language=None)` — копіювання одним
  кліком, без окремого JS.
- **Нуль додаткових Gemini-викликів з боку застосунку** — доступно й
  безкоштовним юзерам, не за paywall.
- i18n: 4 нові ключі (`video_ai_search_title/intro/interest_label/
  interest_placeholder`) — en/uk написані вручну (та сама конвенція, що й
  для `target_grammar`); решта 12 мов чекають на прогін
  `scripts/generate_i18n_strings.py` наступної сесії (потрібен
  `GEMINI_API_KEY`, якого в пісочниці немає) — до того часу тихо
  фолбечаться на англійську через існуючий механізм `_load_generated()`.
- **Перевірено**: `py_compile` на обох змінених файлах;
  `_video_ai_search_prompt()` прогнано ізольовано (AST exec) на двох
  парах мов/рівнів (Spanish/Ukrainian/B2 без інтересу — лишився
  плейсхолдер; Korean/English/A2 з інтересом "cooking" — підставився
  коректно). **Не перевірено вживу через реальний Streamlit UI** — Фаза 5
  вимагає пройти урок до кінця через реальний Google-логін, якого в
  пісочниці немає (той самий, вже задокументований обмежувач для всіх
  live-Gemini фіч проєкту).

## Живий UI-тест target_grammar через Claude in Chrome + реальний баг у engine/db.py (2026-08-23, продовження)

Наталья попросила перевірити фічу через "claude chrome" — її реальний, вже
залогінений Chrome (той самий акаунт, що й на скріншоті — uk→es, Lesson
91/182). Пройшла Phase 3 → "Граматика цієї мови" → Ser vs. Estar реальними
кліками (не Python-скриптом): переклала 5 речень, "Перевірити" — усі 5
правильні, UI відрендерився коректно (заголовок з gloss_en, переклад на
українську підвантажився живим Gemini-викликом).

**Знайдений і виправлений реальний production-баг**: перевірка mastery/
srs_state одразу після цього показала 0 рядків для `target_grammar` —
запис не відбувся, хоча UI показав правильні відповіді без жодної помилки
де-небудь. Причина (діагностувала напряму, не здогадкою): `engine/
db.py::get_engine()` тримає ОДИН SQLAlchemy Engine на весь час життя
Streamlit-процесу (`@st.cache_resource`) — той самий Supabase pooler, який
уже раз обривав з'єднання під час масового запису
(`scripts/seed_content_units.py`, задокументовано раніше), в довгоживучому
процесі так само рве з'єднання всередині одного виклику, і
`record_result()`'s `except Exception: pass` тихо це ковтав.

Виправлено **в корені, не заплаткою для одного виклику**: `engine/db.py`
тепер має `_with_retry()` — обгортку з 3 спробами (`OperationalError`,
пауза 0.5с/1с/1.5с), застосовану до `fetch_all`/`execute`/`execute_many` —
тобто до КОЖНОГО виклику БД у проєкті (recommender, gamification, billing),
не лише до нового `target_grammar` коду.

**Перевірено насправді**: тимчасова діагностика через `st.session_state`
(звичайний `st.error()` виявився марним — `st.rerun()` стирає його до того,
як встигаєш побачити, бо весь UI перемальовується заново на наступному
прогоні), прогнала той самий Ser vs. Estar через реальний клік ще двічі.
Перша спроба (до фіксу в `db.py`) — підтверджено 0 записів. Дві спроби
після фіксу — обидві успішні (`n_attempts=10` у mastery = точно 5+5 з цих
двох прогонів, перша спроба справді не потрапила в підсумок). `srs_state`
теж підтверджено: `interval_days=60, due_date=2026-10-22, last_result=True,
reps=10`. Діагностичний код прибрано з `grammar.py` після підтвердження,
лишився тільки чистий `except Exception: pass` (той самий стиль, що вже в
`LessonSession.score()`).

**Не зроблено**: реальний запис (mastery/srs_state на її акаунті) із цієї
перевірки НЕ прибирала — це не тестовий фікстур, а справжні (нею дійсно
клікнуті, дійсно правильні) відповіді на її реальному
`nataliiamaslovaneo@gmail.com`. Якщо вона хоче це прибрати —
`DELETE FROM mastery/srs_state WHERE target_lang='Spanish' AND
(module='target_grammar' OR unit_id LIKE 'target_grammar:%')`, не робила
без прямого запиту.

## Как работать

Двигайся по фазам по порядку — A и B можно делать почти сразу и параллельно, они не
пересекаются. C — самая содержательная и рискованная часть, стоит согласовать схему
данных с Наташей до реализации, а не после. Перед каждой новой фазой — коротко
резюмируй, что сделано, и спроси, продолжать ли в эту сторону — часть решений здесь
(например провайдер STT в фазе B, конкретная модель ценообразования в фазе D)
Наталья ещё не зафиксировала окончательно.

## Мовні шляхи стають справжніми уроками Grammar (2026-08-23, продовження)

Наталья попросила: теми з `engine/target_grammar_paths.py` мають бути не окремою
опцією в Practice, а справжніми уроками — наприклад, українські відмінки в самій
категорії "Nouns & Quantities" разом зі 173 оригінальними уроками. Зроблено
наскрізно, на реальній Supabase + живому Streamlit-сесії Наталі (Claude in Chrome).

**Ключова архітектурна ідея**: `lesson_id 1000-1074` (постійні, вручну призначені —
див. докстрінг `engine/target_grammar_paths.py`, ніколи не перенумеровувати) + нове
поле `category` (одна з 7 існуючих категорій `_GRAMMAR_CATEGORIES`) на кожній темі.
`engine/target_grammar_loader.py::build_grammar_lesson_rows()` перетворює тему на
рядки `{lesson_id, phrase_id, difficulty, native, target, topic_en}` — той самий
формат, що й `engine.loader.load_phrases()` — і `grammar.py::_load_grammar()`
конкатенує їх до звичайних 182 уроків. Це означає: Category-фільтр, Select Lesson,
snake-path, Phase 1-8, mastery/SRS — усе працює без жодного окремого коду для цих
уроків, вони просто інші рядки в тому самому df.

**Мовна ізоляція** — той самий трюк, що вже використала для `target_grammar`-модуля:
`content_units.source_lesson = <lang_code>` (а не lesson_id як для звичайних уроків)
локає юніт до однієї цільової мови через уже наявний фільтр у
`recommender._candidates()` — жодних змін у самому фільтрі не знадобилося.

**Реальна проблема продуктивності, знайдена вживу**: перша версія перекладала
`native` ЖАДІБНО при побудові df для picker'а — до 56 живих Gemini-викликів
(7 тем × 8 речень) підряд, синхронно, ДО того як міг відрендеритись навіть
Select Lesson dropdown. Підтверджено наживо: сторінка зависла на **понад
хвилину**, довелося закрити вкладку й відкрити нову. Виправлено архітектурно —
`build_grammar_lesson_rows()` більше НЕ перекладає, ставить `native = target`
як плейсхолдер + `native_needs_translation=True`; переклад тепер лінивий,
обмежений ОДНИМ уроком (~8 рядків), відбувається або в прев'ю (нова функція
`translate_rows_native()`, викликається лише для обраного в дропдауні уроку),
або при реальному відкритті уроку (`engine/session.py::_fill_missing_translations`
розширено новою гілкою поруч із вже наявною CEFR-J-гілкою). Побудова df тепер
0.0с (перевірено), переклад — лише коли справді потрібен.

**Другий реальний баг, знайдений вживу**: прогрес-бар "YOUR PATH" у сайдбарі
показав **"999 done · -810 to go · 528.6%"** для щойно відкритого уроку 1000 —
`grammar.py`'s `completed_lessons = state.lesson_id - 1` мовчки припускав, що
lesson_id — це послідовна позиція 1..N, що ламається для lesson_id≥1000.
Виправлено: `completed_lessons` тепер — позиція `lesson_id` у ВІДСортованому
списку всіх уроків пари (index-based, не арифметика по значенню id).
Перевірено прямим розрахунком тим самим кодом: для англ→укр `lessons.index(1000)
== 182` з `total=189` → 96.3%, коректно.

**Перевірено вживу, реальними кліками на акаунті Наталі** (English→Ukrainian):
Category "Nouns & Quantities" тепер показує **21 урок замість 19** — знайдено
"Рід іменників і узгодження прикметників (Gender agreement) · A1" і "Відмінки...".
Прев'ю з реальним живим перекладом на англійську підвантажилось швидко
("This new table stood there." тощо). "Start Lesson" реально запустив повний
Phase 1-8 flow (Warmup з живим Gemini-питанням). Побутово підтверджено й
попередні фікси того ж сеансу: "a big dog" → "велика собака" (фем., виправлення
з LINGUISTIC_AUDIT.md п.2.1 працює й тут), для uk→es пари recommender сам
запропонував "Уроку 1021" (новий іспанський subjunctive) через банер рекомендацій.

**Не зроблено**: категорії обрані мною (наприклад "aspect" → "past", "ser/estar" →
"basics") — розумний перший прохід, не звірений з Наталею предметно; можна
перенести окремі теми в іншу категорію пізніше, зміна `category` в реєстрі не
зачіпає вже засіяні content_units (`topic`/`level` там не залежать від категорії).

## Ідея А реалізована: пояснення правила у Step 1 (2026-08-23, продовження)

Наталія уточнила ідею А з попереднього розділу: не довільний чат, а конкретно
"Новий матеріал" (Step 1 "Read out loud") зараз показує лише список фраз без
жодного пояснення — додати правило-пояснення-приклади-виключення через Gemini,
кешоване для повторного використання. Тільки Premium.

**Ключова зміна дизайну відносно початкової ідеї з лімітом 5/день**: оскільки
контент Step 1 однаковий для КОЖНОГО студента, що відкриває цей урок (той самий
`lesson_id` → той самий набір фраз), пояснення правила кешується персистентно
в Postgres за ключем `(topic, level, target_lang, native_lang)` — той самий
патерн "оплачується раз на весь проєкт", що вже застосований у
`translate_phrase`/`phrase_translations`. Це означає: ліміт на день не потрібен
узагалі — перший студент, що відкриє урок 42 (Present Continuous,英→укр),
платить один живий Gemini-виклик; усі наступні (будь-який студент, будь-коли,
навіть після редеплою) отримують закешовану відповідь безкоштовно. `@_require_paid`
лишається — це продуктовий гейт (Premium-фіча), не лише спосіб контролю
вартості.

**Зроблено:**
- [schema.sql](schema.sql) — нова таблиця `lesson_explanations`
  (`explanation_hash` PK = sha256(target_lang|native_lang|level|topic),
  `explanation` JSONB) — той самий шейп, що й `phrase_translations`.
- [engine/gemini.py::explain_lesson_rule()](engine/gemini.py) — новий
  `@_require_paid @_cache.memoize()` виклик, два шари кешу (local diskcache +
  Postgres, як у `translate_phrase`). Бере `seed_phrases` (реальні речення
  уроку) лише для контексту — просить Gemini написати НОВІ 2-4 приклади, не
  повторювати урок дослівно (той самий "for reference only" патерн, що й
  `generate_practice_test`). Повертає `{"rule": str, "examples": [{"target",
  "native"}], "exceptions": [str]}`.
- [grammar.py::_render_rule_explanation()](grammar.py) — новий `st.expander`
  "📖 Пояснити правило" у `step1()`, гейтований на `module == "grammar"`
  (vocab/phrasebook не мають "правила" в тому ж сенсі — залишено на потім,
  якщо підтвердиться цінність). `try/except PaidFeatureRequired → _show_upsell`,
  той самий патерн, що й у решти 13 місць виклику Gemini в grammar.py.
  Session-state ключ включає `lesson_id` (`s1_explanation_{lesson_id}`), щоб
  застаріле пояснення з іншого уроку не могло показатись під новим.
- i18n: 3 нові ключі (`rule_explanation_title/btn`, `rule_exceptions_label`) —
  en/uk вручну; решта 12 мов — той самий борг, що й для `video_ai_search_*`
  вище, чекає на `generate_i18n_strings.py` наступної сесії.

**Знайдений і обійдений підводний камінь** (не з моєї фічі, вже існуючий
дефект): очевидний спосіб узяти назву теми уроку — `getattr(session.state,
"topic", "daily life")` — саме так зроблено у Phase 3/4 (`grammar.py`, коло
рядків 2328/2569). Виявилось: `SessionState` (`engine/session.py`) взагалі
не має поля `topic` — цей `getattr` **завжди** повертає "daily life",
для КОЖНОГО уроку. Для Phase 4 це реальний баг (тема йде прямо в промпт
`generate_open_question`: "the student just studied the topic 'daily
life'" — завжди, незалежно від реального уроку); для Phase 3 вплив менший
(використовується лише коли `phrases` порожні). Для власної фічі я цей
патерн НЕ використала — натомість `get_grammar_topics(session.df,
native_lang=native_lang).get(state.lesson_id, "")`, та сама функція, яку
вже використовує пікер уроків (`grammar.py:1555`), читає реальну колонку
`topic_{lang_code}`, вже завантажену в `session.df`. **Перевірено сумісність
із сьогоднішніми "мовними шляхами"** (розділ вище, lesson_id 1000+): їхні
рядки мають лише `topic_en` — `get_grammar_topics`'s власний фолбек на
`topic_en` (вже існує, не мій код) покриває це без додаткових змін.
Сам баг у Phase 3/4 не чіпала (поза заявленим скоупом) — залишила
suggestion-чіп для окремої сесії.

**Не перевірено вживу** — та сама відома межа пісочниці (немає реального
`GEMINI_API_KEY`/логіну/`DATABASE_URL`). Перевірено ізольовано: `raw =
explain_lesson_rule` (розгорнутий через `__wrapped__` повз обидва
декоратори) з підміненим `_model` — промпт коректно містить тему й приклад
речення уроку, JSON-парсинг результату працює, DB-кеш gracefully повертає
`None`/мовчки не зберігає без `DATABASE_URL` (як і `translate_phrase`).
`py_compile` проходить на всіх змінених файлах.

**Потрібно від Наташі перед реальним використанням**: прогнати оновлений
`schema.sql` (нова таблиця `lesson_explanations`) на живій Supabase — те ж
саме `CREATE TABLE IF NOT EXISTS`, безпечно перезапускати, як і в Фазі D.

## Practice автопідхоплює тему з New Material для "мовних шляхів" (2026-08-23, продовження)

Наталія показала скрін: відкрила урок 1001 (одна з нових "мовних шляхів"
lessons, розділ вище), у Phase 3 вручну вибрала test type "Language-specific
grammar" і вручну підняла правильну тему в другому селекторі — попросила,
щоб це підхоплювалось само, без ручного вибору.

**Скористалась уже наявним реверс-лукапом**, не писала нового:
`engine/target_grammar_paths.py::topic_for_lesson_id(lesson_id)` вже існував
саме для цього (докстрінг: "used by engine.target_grammar_loader /
engine.picker to resolve one of these synthetic lesson_ids back to its topic
dict") — просто ще не був підключений до Phase 3 picker'а.

- [grammar.py::phase3_practice()](grammar.py) — одразу після обчислення
  `_target_grammar_topics`, новий блок: якщо поточний `session.state.lesson_id`
  має відповідний запис у реєстрі (тобто це сам "мовний шлях" урок, не
  один зі 173 оригінальних), заздалегідь підставляє значення в
  `st.session_state["p3_type"]`/`["p3_tg_topic"]` ДО того, як відповідні
  `st.selectbox` створюються — Streamlit бере вже наявне значення в
  session_state за тим самим `key` як початкове замість дефолтного
  першого пункту.
- **Гейт "один раз на урок"** (`p3_auto_lesson_id` session-key): підстановка
  відбувається лише коли `lesson_id` змінився з попереднього рендеру — інакше
  студент не зміг би вручну переключитись на інший test type чи іншу тему
  для практики (кожен наступний rerun знову перезаписував би вибір). Той
  самий "reset per lesson_id" підхід, що й `s1_explanation_{lesson_id}` вище.
- Захист від цілком гіпотетичного міжмовного збігу lesson_id: звіряє
  `_auto_topic["target_lang"] == target_lang` перед підстановкою (реально
  статись не може — синтетичний урок з'являється лише в df тієї мови, для
  якої його склеїв `build_grammar_lesson_rows`, — але дешева перевірка).
- **Перевірено ізольовано** (без Streamlit-рантайму): для lesson_id 1001
  (Ukrainian) — коректно резолвиться в "uk_cases_basic" / "Відмінки:
  називний, знахідний, родовий..." (індекс 1 у списку); для lesson_id 1000 —
  "uk_gender_agreement" / "Рід іменників..." (індекс 0); звичайний урок
  (id 42, не в реєстрі) — `None`, без падіння. `py_compile` проходить.
  **Не перевірено вживу через реальний UI** (та сама відома межа
  пісочниці — потрібен реальний логін і сесія).

## Жива перевірка "Explain the rule" через Claude in Chrome + 2 реальних баги (2026-08-23, продовження)

Наталія: фіча блокує безкоштовний акаунт (підтвердила окремо), пояснення
змістовні, але **~1 раз з 5 плутало рідну/цільову мову** — для пари
English→Ukrainian починало розповідати про англійську замість української.
Попросила перевірити через Claude in Chrome (її реальний залогінений
Chrome, Premium-акаунт), доробити i18n для нових ключів, і підтвердила,
що прогнала `schema.sql` на живій БД.

**Розслідування — 2 реальних баги, обидва знайдені й виправлені:**

1. **`lesson_explanations` насправді не було створено.** Наталіїн запуск
   `schema.sql` не долетів до цієї конкретної таблиці (стара копія файлу,
   найімовірніше) — перевірила напряму `SELECT ... information_schema.tables`,
   таблиці не існувало. Створила сама (`CREATE TABLE IF NOT EXISTS`, безпечно,
   адитивно) — підтверджено запитом. Це також пояснює "1 з 5" — кешування
   взагалі не працювало на рівні БД, кожен клік був окремим незалежним
   Gemini-викликом, тож розбіжність — це просто дисперсія моделі, не баг
   кешу.
2. **Корінь плутанини мов — сам промпт, не гейт/дані.** `topic` для
   оригінальних 182 уроків — це англійська граматична категорія (напр.
   "Phrasal verbs"), навіть коли target_lang інший (LINGUISTIC_AUDIT.md,
   те саме структурне спостереження). Просила Gemini "explain this grammar
   point in {target_lang}" з міткою, яка сама по собі — назва англійського
   явища — і модель іноді просто починала розповідати про англійську,
   ігноруючи target_lang. **Живий репро**: урок "Phrasal verbs (everyday
   actions) · B1", English→Ukrainian — відтворила достеменно те, що
   описала Наталія (та сама формула, слово в слово, що й на її скріні,
   бо старий баг лишався в ЛОКАЛЬНОМУ diskcache `.cache/gemini/`, який
   працює незалежно від Postgres — довелось `cache.clear()`, щоб дійсно
   прогнати новий промпт, а не віддати ту саму кешовану погану відповідь).
   [engine/gemini.py::explain_lesson_rule()](engine/gemini.py) — промпт
   переписаний: (а) явно і повторно фіксує, яка мова — target, яка —
   native, "do not swap these"; (б) прямо попереджає, що `topic`-мітка
   може бути запозиченою з англійської термінології, і вимагає пояснювати,
   як САМЕ target_lang виражає цю ідею, а не пояснювати англійську;
   (в) якщо в target_lang немає прямого відповідника — явно сказати це і
   пояснити, що мова робить замість цього. **Перевірено вживу двічі**:
   "Phrasal verbs" (найскладніший кейс — явище, якого в українській
   структурно нема) тепер коректно пояснює українські префіксальні
   дієслова (ви-/за-/під-) як функціональний аналог, англійською, з
   українськими прикладами; "Hypothetical situations" (Second Conditional)
   — так само коректно, українською граматикою умовного способу.
3. **Побічний баг, знайдений при живій перевірці бага №1** (не Наталіїн
   репорт, знайшла сама під час діагностики): `_save_lesson_explanation_to_db()`
   викликала `db.upsert()` БЕЗ `touch_updated_at=False` — на відміну від
   `_save_translation_to_db()` (той самий патерн, `phrase_translations`),
   де цей параметр є. `lesson_explanations` не має колонки `updated_at`
   (на відміну від решти таблиць схеми, для яких `db.upsert()` цю колонку
   й розраховує рефрешити) — кожен запис ловив
   `psycopg2.errors.UndefinedColumn`, тихо проковтувалось власним
   `except Exception: pass`. Тобто кеш-шар Postgres не працював ЗОВСІМ,
   навіть після створення таблиці. Виправлено — додано
   `touch_updated_at=False`. **Перевірено вживу через справжній застосунок**
   (не ізольовано): клік "Explain this rule" на "Hypothetical situations"
   реально записав рядок у `lesson_explanations` (перевірено прямим SQL-
   запитом одразу після кліку — topic/level/target_lang/native_lang
   збіглися).

**i18n для нових ключів — зроблено реальним прогоном** (не заглушка):
`scripts/generate_i18n_strings.py` (справжній `GEMINI_API_KEY`, доступний
у цій сесії) згенерував усі 7×12=84 відсутні рядки (`rule_explanation_*`,
`video_ai_search_*`) для решти 12 мов, записано в
[data/i18n_generated.json](data/i18n_generated.json). Перевірено
програмно — `i18n.get()` віддає реальний переклад для Spanish/Japanese/
Chinese/German, не fallback на англійську.

**Не перевірено**: інші 4 з "5 спроб" Наталії — чи повторюється плутанина
мов зараз хоч зрідка (LLM лишається стохастичним, повної гарантії 0%
немає), і чи є подібна плутанина в парах, де native_lang теж не English
(наприклад Ukrainian→Spanish) — перевіряла тільки English↔Ukrainian.

## Рішення: "порожні за метою" уроки НЕ прибираються з path (2026-08-23, продовження)

Наталія спитала про дзеркальну проблему до "мовних шляхів": розділ 1
LINGUISTIC_AUDIT.md показав, що частина з 182 оригінальних уроків (напр. 35
"Locations with articles", 167–170 "Verb + infinitive/gerund", 181 "Causative",
182 "It+adj+to-infinitive") формально не мають граматичної мети для мов без
відповідного явища (uk/ru/pl/ko без артиклів і т.д.) — чи варто їх прибирати
з path саме для цих мов?

**Вирішено — ні, не прибирати.** Аргументація: фрази цих уроків усе одно
реально навчають (напр. урок 35 без артиклів коректно відпрацьовує
прийменниково-відмінкові конструкції), видалення вимагало б окремого
класифікаційного проходу по 182 темах × 13 мовах (дзеркало вже зробленого
аудиту), і головне — свіжий фікс `explain_lesson_rule()` (Step 1 "Explain the
rule", вище) уже пом'якшує саме цю плутанину: коли тема нерелевантна для
target_lang, явно каже, чого в цій мові немає і що є замість цього, а не
мовчки навчає неіснуючого явища. Зафіксовано в
[LINGUISTIC_AUDIT.md](LINGUISTIC_AUDIT.md) розділі 1 як свідоме рішення —
курикулум лишається "англомовним скелетом" без фільтрації за мовою.

## Прибрано 2 тестові хвости з живої БД (2026-08-23, продовження)

Наталія попросила почистити обидва пункти, залишені в бэклозі раніше.

- **Тестові mastery/srs_state (Ser vs. Estar, Spanish)** — прев'ю-запит
  підтвердив рівно ті самі рядки, що описані вище (`n_attempts=10`,
  `unit_id='target_grammar:es:es_ser_estar'`). Видалено обидва рядки
  (`DELETE ... WHERE user_id='nataliiamaslovaneo@gmail.com' AND
  module='target_grammar'` / `unit_id LIKE 'target_grammar:%'`) —
  підтверджено порожнім результатом після.
- **Дублікат Stripe-клієнта** — перевірила, що ключ дійсно `sk_test_`
  (тестовий режим), перш ніж щось чіпати. Обидва клієнти виявились з
  АКТИВНОЮ підпискою кожен (не один пустий, як здавалось спочатку) —
  звірила з `subscriptions`-таблицею в БД, яка реально прив'язана до
  `cus_V6cwPtZ5DgiIqd` (`sub_1U6PgTJxQf9C7MEL6DcjZPmk`). Зайвий
  `cus_V6cuwuOtMdbxk7` — скасувала його підписку
  (`sub_1U6PenJxQf9C7MELY0TSzqA1` → `canceled`), потім видалила самого
  клієнта через Stripe API. Реальний клієнт/підписка (`cus_V6cwPtZ5DgiIqd`,
  `active`) перевірено — не зачеплений.

## Виправлено "daily life" — Phase 3/4 підставляли неіснуюче поле (2026-08-23, продовження)

Наталія попросила пояснити раніше знайдений баг ("тема завжди 'daily
life'") і виправити. Корінь виявився ширшим, ніж думала спершу (раніше
запропонувала фікс тільки для Grammar як окремий чіп-таск, тепер зробила
сама й повністю):

- `getattr(session.state, "topic", "daily life")` — `SessionState`
  (`engine/session.py`) взагалі не має поля `topic`, тому `getattr`
  ЗАВЖДИ повертав "daily life", для кожного уроку, кожного модуля. Гірше
  всього для Phase 4 — `generate_open_question()` вставляє тему прямо в
  промпт Gemini ("the student just studied the topic '{topic}'"), тобто
  питання для розмовної практики завжди було відірване від реального
  уроку.
- **Реальна тема лежить у РІЗНИХ місцях залежно від модуля** — саме тому
  один `getattr` ніколи не міг спрацювати правильно: у Grammar це колонки
  `topic_{код_мови}` на `session.df` (те саме, що вже коректно читає
  Step 1's "Explain the rule"); у Vocab/Phrasebook — звичайний ключ
  `"topic"` прямо на кожній фразі, АЛЕ для CEFR-J Vocabulary той самий
  ключ `"topic"` — це просто код CEFR-рівня (A1..C2), не реальна тема
  (той самий розподіл, який `_lesson_level()` уже робить рядком вище).
- [grammar.py::_current_topic()](grammar.py) — новий спільний хелпер,
  розводить усі три випадки: Grammar → `get_grammar_topics(session.df,
  native_lang=...)`; Vocab/Phrasebook → `phrases[0]["topic"]`, якщо це не
  CEFR-код; інакше — фолбек "daily life" (як і раніше, не гірше — CEFR-J
  Vocabulary справді не має тематичного угруповання, окрім рівня, це не
  моя фіча ламає, а вже наявне обмеження даних).
  `phase3_practice()`/`phase4_expression()` тепер викликають цей хелпер
  замість зламаного `getattr`.
- **Перевірено ізольовано** (AST exec, без Streamlit-рантайму): Grammar
  з native=Ukrainian → реальна українська назва теми; native=English →
  коректний фолбек на `topic_en`; Phrasebook → реальна назва розділу
  ("Greetings, Basics & Courtesy"); CEFR-J Vocabulary (`topic="A2"`) →
  коректно розпізнано як не-тему, фолбек "daily life"; порожній список
  фраз → фолбек без падіння. `py_compile` проходить.
  **Не перевірено вживу через реальний UI** — та сама відома межа
  пісочниці.

## Code review перед Фазою E: 2 реальних краші виправлено + 5 менш критичних знахідок (2026-08-24)

Наталія попросила прогнати рев'ю якості коду на весь діапазон `da01c8d~1..HEAD`
(усе сьогоднішнє: мовні шляхи, Explain the rule, AI-пошук відео, book-паттерни,
фікс daily life) перед тим, як переходити до Фази E. Три паралельні агенти,
8 кутів (correctness × 3, reuse/simplification/efficiency, altitude,
conventions).

**Виправлено одразу (обидва — реальні краші, не здогадки):**
1. **`engine/gemini.py::_lesson_explanation_from_db()` — Postgres-кеш для
   "Explain the rule" НІКОЛИ не спрацьовував.** `json.loads(row["explanation"])`
   на JSONB-колонці, яку psycopg2 вже сам десеріалізує в dict — `TypeError`,
   тихо проковтнутий `except Exception: return None`. Тобто вчорашня "жива
   перевірка" (клік → рядок з'явився в БД) підтвердила лише ЗАПИС, не читання
   — кожен наступний клік і далі платив за живий Gemini-виклик. Виправлено
   (`return row["explanation"]`, без `json.loads`) і **перевірено наскрізно**:
   підмінений `_model` фіксує 0 живих викликів при повторному запиті того ж
   уроку — кеш дійсно тепер влучає.
2. **`path_app.py` — "My Path" падає з `KeyError`, якщо рекомендер
   порекомендує `target_grammar`-юніт.** `parse_unit_id()` для
   `target_grammar:<lang>:<topic_key>` повертає dict БЕЗ ключа `lesson_id`
   (тільки `lang_code`/`topic_key`) — а `path_app.py` рендерить кожен юніт як
   урок із номером, `parsed["lesson_id"]` падає до будь-якого кліку. Це не
   гіпотетично: CLAUDE.md вище прямо документує, що recommender РЕАЛЬНО
   порекомендував target_grammar-тему живому юзеру (uk→es, банер). Виправлено
   локально в `path_app.py` (не чіпала спільний `recommender.py`, щоб не
   зачепити банер в grammar.py, який з target_grammar явно працює коректно):
   фільтрує `target_grammar`-юніти з `stats["upcoming"]`/`current_unit"` — та
   сама тема все одно доступна як звичайний урок `grammar:1000+`, тож
   рекомендація не губиться, просто "My Path" вибирає launchable форму.

**Знайдено, не виправлено (передано на рішення):**
3. `app.py::_module_progress` — той самий "daily life"-типу баг, що вже
   пофіксили в бічній панелі уроку: `min(saved_lesson, total)` на картці
   лаунчера дає хибні 100% для lesson_id≥1000, бо там не index-based підрахунок.
4. `grammar.py:2452` — `p3_type`/`p3_tg_topic` session_state не скидається
   при зміні уроку/мови — потенційний виняток Streamlit, якщо новий
   target_lang має коротший список тем.
5. `grammar.py:2607` — Check для `target_grammar` робить до 20 послідовних
   DB-запитів за один клік (по 4 на кожен з 5 items) замість агрегації.
6. `grammar.py:795` — `_render_rule_explanation` дублює лукап теми замість
   виклику вже готового `_current_topic()` (той самий ризик розсинхрону, що
   й у вже пофікшеному "daily life" баг).
7. `engine/gemini.py:627` — `generate_target_grammar_drill` копіює промпт
   `generate_lesson_construction_drill` замість спільного хелпера (визнано
   в докстрінгу самої функції, не прибрано).

Повний список — `ReportFindings` виклик цієї сесії. `py_compile` проходить
на обох фіксах.

## 2 нові фічі: "Explain this topic" у My Path + список слів у Vocabulary (2026-08-24)

Наталія: "в my path нет Обьясни єту тему для нового материала. добавь. в
vocabulary можна давать что-то типа Обьясни тему - со списком слов с
переводом которіе будут отрабатіваться - headwords зліва target language -
справа родной."

**1. My Path → "Explain this topic" на картці рекомендованого Grammar-уроку**
([path_app.py::_render_topic_explanation()](path_app.py)) — та сама
`explain_lesson_rule()` з Step 1, викликана з `seed_phrases=[]` (My Path не
завантажує df уроку, тільки summary-рядок з `content_units`; функція вже
коректно обходиться без seed_phrases — той самий кеш-ключ
`(topic, level, target_lang, native_lang)`, тож ділить кеш зі Step 1).
Grammar-only, той самий гейт, що й Step 1. Ключ session_state —
`path_explanation_{unit_id}`, щоб стара відповідь не лишалась при зміні
рекомендації.

**2. Vocabulary → "📚 Word list for this lesson" у Step 1**
([grammar.py::_render_vocab_wordlist()](grammar.py)) — CEFR-J Vocabulary
тільки (єдиний loader з полем `headword`). Перекладає кожен унікальний
headword окремо через уже наявний `translate_phrase()` (не нову функцію) —
headword завжди англійською (CEFR-J джерело), тому це рівно та сама форма
"перекласти одне слово", яку `translate_phrase` вже вміє й кешує в Postgres.

**Попутний дрібний фікс** — `_render_rule_explanation()` (Step 1, Grammar)
дублювала лукап теми замість виклику `_current_topic()` (Finding #6 з
рев'ю вище) — прибрала дублювання, раз вже редагувала цей код.

**Знайдено й виправлено дорогою: сервер на порту 8501 був застряглий зі
старим кодом у пам'яті.** Жива перевірка "My Path" одразу після фіксу
target_grammar-краху (розділ вище) впала З ТОЮ Ж ПОМИЛКОЮ (`KeyError:
'lesson_id'`) навіть після повного `navigate` (не просто `st.rerun()`).
Пряме відтворення тим самим кодом у Python-скрипті показало 0 помилок —
логіка фільтра коректна. `ps aux` показав: процес на 8501 (`venv`, Python
3.11) стартував о 18:03, задовго до всіх сьогоднішніх правок — `runOnSave`
чомусь не підхопив зміни в `path_app.py` (можливо специфіка вкладеного
імпорту через `app.py`, не досліджувала глибше). **Перезапустила сервер**
(`kill` старого PID + новий `streamlit run app.py --server.port 8501` на
тому ж `venv`) — після цього обидва краш-фікси і обидві нові фічі
підтверджено вживу без жодної помилки. **Урок для наступної сесії**: якщо
жива перевірка показує баг, який щойно мала бути виправлена — спершу
перевірити вік процесу (`ps aux | grep streamlit`), не одразу вважати фікс
невдалим.

**Перевірено вживу через Claude in Chrome** (реальний акаунт, English→Ukrainian):
- My Path: рекомендація "Not having — he/she" (Grammar, Lesson 15) →
  "Explain this topic" → коректне пояснення конструкції "у + Genitive +
  немає" (заперечення присвійності в українській), англійською, з
  прикладами — прямо з картки, без входу в урок.
- Vocabulary: Unit "twenty, glasses, six" (A1) → "Show word list" →
  коректний список: twenty→двадцять, glasses→окуляри, six→шість,
  many→багато, listen→слухати, Mr./Mr→Пан/Пан, city→місто, build→будувати
  — саме окремі слова, не речення.

## Рев'ю перед Фазою E (2) + старт Фази E (PWA-упаковка) (2026-08-24, продовження)

Запит Наталі: прочитати CLAUDE.md, перевірити проєкт на чистоту/баги, і
переходити до Фази E. На момент старту в робочій директорії вже були
незакомічені зміни від паралельної сесії (11 інших сесій `verbashake` в
цей момент активні одночасно, per `ListAgents`) — нова фіча "❓ Чому так
побудовано?" (`engine/rate_limit.py`, `explain_phrase_part` у
`engine/gemini.py`, `_render_confusing_part_helper` у `grammar.py`) +
фікс position-based нумерації уроків (`app.py::_module_progress`) + новий
`_sidebar_lesson_name`, ще не задокументовані в цьому файлі. Прочитала їх
через `git diff HEAD` перед тим, як щось міняти.

**Рев'ю коду** (8 паралельних агентів-кутів — correctness×3, reuse,
simplification, efficiency, altitude, conventions — на весь незакомічений
diff): 9 знахідок пройшли верифікацію, 4 виправлено одразу, 5 передано на
рішення пізніше (`ReportFindings` цієї сесії):

**Виправлено:**
1. **Rate-limit для "❓ Чому так?" списував денну спробу навіть на
   безкоштовний cache hit.** `explain_phrase_part()` сам перевіряє
   Postgres-кеш (`phrase_explanations`) і повертає закешовану відповідь
   без живого виклику Gemini — але `grammar.py` викликав
   `rate_limit.check_and_increment()` ДО цієї перевірки, тобто повторне
   питання про вже пояснений фрагмент (своє чи чуже) все одно з'їдало
   1/10 денного ліміту, прямо суперечачи власному докстрінгу фічі
   ("every distinct string is a fresh cache miss"). Додано
   `engine/gemini.py::phrase_explanation_is_cached()` — публічний "peek"
   у той самий Postgres-кеш без побічних ефектів і без `@_require_paid`
   (перевірка кешованості сама по собі нічого не коштує, тому не має
   блокувати навіть безкоштовного юзера від відповіді на "чи це
   безкоштовно"). `grammar.py` тепер викликає rate-limit лише коли
   `already_cached` — `False`.
2. **`app.py::_grammar_lesson_ids()` не бачив "мовні шляхи" (lesson_id
   1000+).** Функція викликала голий `load_phrases()` замість
   `grammar.py::_load_grammar()` (яка з 2026-08-23 доклеює
   `target_grammar_paths`-уроки для target_lang). Значить для юзера, чий
   `lesson_pointer` вказує на такий урок (recommender реально туди
   заводить, підтверджено раніше в цьому ж файлі), `saved_lesson not in
   lesson_ids` → `_module_progress` падав на старий, вже відомий баг
   "raw lesson_id замість позиції" → хибні 100%/N-з-N на картці Grammar —
   рівно той самий баг, який цей diff і мав фіксити, просто для іншого
   діапазону id. Виправлено — тепер імпортує й викликає `_load_grammar`.
3. **`data/i18n_generated.json::pq_you_wrote`** — 12 машинно згенерованих
   мов (усі, крім en/uk) загубили пробіл на початку рядка
   (`"— tu as écrit..."` замість `" — tu as écrit..."`) — `app.py`
   конкатенує без роздільника, результат читався б як
   "chat— tu as écrit". Виправлено точковим скриптом (додано пробіл лише
   де його бракувало), JSON лишився валідним.
4. **Власний код цієї сесії, знайдений тим самим рев'ю**: щойно написана
   `app.py::_inject_pwa_head()` (нижче) спершу викликалась на кожному
   Streamlit-rerun (кожен клік будь-де в застосунку), не раз на сесію —
   кожен виклик створював і одразу знищував прихований iframe. Додано
   гейт `st.session_state["_pwa_head_injected"]`.

**Передано на рішення, не виправлено зараз** (реальні, але нижчого
пріоритету/ширші за обсягом для цієї сесії — деталі в `ReportFindings`):
`_module_progress` все ще падає на старий фолбек, якщо збереженого
`lesson_id` взагалі нема у свіжому `lesson_ids` (мертвий/мігрований
покажчик); нумерація уроку показується по-різному в різних місцях того ж
екрану (сайдбар — позиція, "Continuing from"/`phase5_summary`/"Jump to
lesson" — сирий `lesson_id`); дублювання hash+cache-lookup+upsert коду
між `lesson_explanations`/`phrase_explanations`/`phrase_translations`;
rate-limit не атомарний (write потім read); `picker.py`'s
`lang_suffix != "vocab"` — magic-string спецвипадок замість
capability-флагу на `cfg`.

### Фаза E — PWA-упаковка (веб, без сторов)

Зроблено мінімальний, реалістичний обсяг для Streamlit-застосунку:
маніфест + service worker, щоб браузер пропонував "Встановити
застосунок" / "Додати на головний екран". **Свідомо не** offline-first
кеш самого застосунку — кожен екран рендериться через живий
Streamlit-websocket (Gemini-виклики, читання mastery/SRS із БД), тому
"офлайн-урок" не має сенсу; кешувати html/js, якими керує сам Streamlit
(і які він сам версіонує/хешує при редеплої), означало б ризик показати
зламану сторінку після наступного деплою.

**Зроблено:**
- `.streamlit/config.toml` — `server.enableStaticServing = true` (перша
  фіча в репо, якій треба, щоб браузер сам стягнув файл з `./static/`
  напряму, а не через сервер-сайд `st.image`/читання CSS).
- `static/pwa/manifest.json` — назва/іконки/`display: standalone`/
  кольори з Mova-палітри (`--mova-coral` #FF7B6B, `--mova-surface`
  #FAF7F5). **Знайдений і виправлений власний баг ще до живої перевірки**:
  перша версія мала `start_url`/`scope` як `"./"` і іконки як
  `"app/static/pwa/icon-192.png"` — обидва трактуються браузером як
  ВІДНОСНІ до самого URL маніфесту (`.../app/static/pwa/manifest.json`),
  тобто `"./"` резолвився б у `app/static/pwa/` (не корінь застосунку), а
  іконки — у подвоєний шлях `app/static/pwa/app/static/pwa/icon-192.png`.
  Виправлено: `start_url`/`scope` — абсолютні (`"/"`), іконки — прості
  імена файлів (резолвляться відносно директорії самого маніфесту).
- `static/pwa/sw.js` — мінімальний service worker: `fetch`-обробник є
  (потрібен для Chrome-installability), але кеширує cache-first ЛИШЕ
  власні PWA-файли (іконки/маніфест) за явним списком `PRECACHE_URLS`;
  усе інше (сам застосунок, Gemini/DB-трафік) проходить наскрізь у
  мережу без втручання. Scope — `/app/static/pwa/` (не весь `/`, бо
  Streamlit's static-handler не дає виставити заголовок
  `Service-Worker-Allowed`, потрібний браузеру для ширшого scope) —
  достатньо для критерію installability Chrome, який вимагає лише
  наявність контролюючого SW, не контроль усього origin.
- `static/pwa/icon-192.png`/`icon-512.png`/`icon-maskable-512.png`/
  `apple-touch-icon.png` — згенеровані програмно (PIL, білий "V" на
  коралевому тлі, той самий колір, що й primary CTA) — у проєкті не було
  жодного готового логотипу/іконки для перевикористання (перевірено
  `find` по всьому репо).
- `app.py::_inject_pwa_head()` — той самий патерн, що вже є для dark-mode
  тоглу (`components.html`'s iframe → `window.parent.document`, бо
  `st.markdown` не виконує `<script>` з innerHTML): додає
  `<link rel="manifest">`, `<meta name="theme-color">`,
  `<link rel="apple-touch-icon">` у справжній `<head>` і реєструє SW через
  `window.parent.navigator.serviceWorker.register(...)`. Раз на сесію
  (`st.session_state` гейт, фікс власного бага вище), best-effort
  (`.catch(() => {})` — installability не має ніколи блокувати сам
  застосунок).

**Не зроблено / не перевірено**:
- Жива перевірка інсталяції (Chrome DevTools → Application → Manifest,
  чи реально показує prompt "Install VerbaShake") — не мала доступу до
  живого деплою (verbashake.streamlit.app) чи локального HTTPS у цій
  сесії; PWA-installability в Chrome вимагає HTTPS (localhost — виняток,
  теж підходить для тесту). Наступна сесія/Наталя мають перевірити
  вживу після деплою.
- Публікація в Google Play / App Store — свідомо НЕ на цьому етапі (див.
  рішення #6 вище, Фаза F).
- `manifest.json`'s `screenshots` (опційне поле для "richer install UI" в
  Chrome/Android) — не додано, немає готових скріншотів застосунку під
  рукою в цій сесії.

## Жива перевірка через Claude in Chrome + прогнано schema.sql (2026-08-24, продовження)

На прохання Наталі — прогнала `schema.sql` на живій Supabase (нові таблиці
`phrase_explanations`/`daily_feature_usage` з сьогоднішньої фічі "❓ Чому
так?" — обидва `DATABASE_URL`/`GEMINI_API_KEY` виявились доступні в цій
сесії), перезапустила застряглий процес на :8501 (був живий з 9:50, до
всіх сьогоднішніх правок — той самий урок з попередньої сесії: спершу
перевіряти вік процесу), і прогнала все вживу через її реальний залогінений
Claude in Chrome (Nataliia Maslova, Premium, Russian→Ukrainian).

**Phase E (PWA) — підтверджено програмно, наскрізно:**
- `<link rel="manifest">`/`<meta theme-color>`/`<link apple-touch-icon>`
  реально з'явились у справжньому `<head>` (не в iframe).
- `fetch('/app/static/pwa/manifest.json')` → 200, коректний розпарсений
  JSON (усі шляхи іконок резолвляться правильно після вчорашнього
  власного фіксу відносних/абсолютних URL).
- `navigator.serviceWorker.getRegistrations()` → **один SW, `state:
  "activated"`**, scope `/app/static/pwa/` — критерій installability
  Chrome (валідний маніфест + активний SW) виконано.
- Живий `chrome://apps`-рівня install-промпт (іконка в омнібоксі) не
  перевіряла — інструменти читають вміст сторінки, не хром браузера;
  програмна перевірка вище — найсильніше досяжне підтвердження в цій
  сесії.

**Знайдений і виправлений реальний баг у власному сьогоднішньому фіксі
"rate-limit на cache hit"** — під час живої перевірки через Claude in
Chrome (Vocabulary, урок "dish, am, doll", речення "Я не їм цю страву.",
фрагмент "не їм"): відповідь Gemini прийшла миттєво й коректно, але
`daily_feature_usage.count` виріс, а рядок у `phrase_explanations` НЕ
з'явився. Розслідувала напряму (не здогадкою) — `explain_phrase_part`'s
власний диск-кеш (`diskcache`, `.cache/gemini/`, окремий від Postgres,
безстроковий) вже мав закешовану відповідь на РІВНО цей же
(target_phrase, confusing_part, target_lang, native_lang) — підтверджено
`__cache_key__` + пряма перевірка `key in gemini._cache`. Це артефакт ще
ДО того, як я прогнала `schema.sql` у цій сесії: якась із паралельних
сесій (у момент старту активних було 11, per `ListAgents`) вже тестувала
рівно цю фразу-приклад раніше сьогодні, Gemini відповів, диск-кеш
зберіг відповідь локально — а спроба зберегти в Postgres тоді мовчки
провалилась, бо таблиці `phrase_explanations` ще не існувало. Диск-кеш
не має TTL, тому кожен наступний виклик з тим самим набором аргументів
(навіть із зовсім іншого процесу — диск-кеш файловий, не in-memory)
назавжди повертає закешоване значення, минаючи саме тіло функції — а
значить і мій сьогоднішній `phrase_explanation_is_cached()`-peek (який
дивиться тільки в Postgres) не бачить цей шар і дозволяє
rate-limit-перевірці пройти як для "живого" виклику.

**Підтверджено іншим, гарантовано нефрагментом фрагментом** ("цю
страву" замість "не їм", той самий урок): повний цикл спрацював
правильно — `daily_feature_usage.count` 2→3 (рівно +1, справжній живий
Gemini-виклик), рядок реально з'явився в `phrase_explanations`.
Повторний клік ТІЄЮ Ж фразою одразу після — `count` лишився 3 (не
виріс), пояснення прийшло миттєво — фікс "не списувати квоту за
Postgres-кешований hit" підтверджено робочим наскрізно, реальними
кліками, не логічним тестом.

Не чіпала сам механізм диск-кешу (він і так — свідомий, задокументований
патерн у всьому файлі `engine/gemini.py`, не регресія цієї сесії) — це
одноразовий артефакт перехідного періоду (таблиця з'явилась лише
сьогодні), не системна проблема: для БУДЬ-ЯКОГО нового (phrase,
fragment)-запиту з цього моменту диск-кеш і Postgres-кеш будуть
наповнюватись синхронно, як і задумано.

**Прибрано після перевірки**: `daily_feature_usage` — видалено рядок
тестового рахунку (3 спроби з цієї сесії, не органічне використання) —
`DELETE ... WHERE user_id='nataliiamaslovaneo@gmail.com' AND
feature='explain_phrase_part'`. `phrase_explanations`'s 2 рядки
("не їм"/"цю страву") НЕ видаляла — це реальний, корисний закешований
контент (майбутній студент, що спитає про той самий фрагмент, отримає
безкоштовну відповідь), а не тестовий сміттєвий рядок, шкоди не робить.

**Побічно підтверджено вживу (не моя фіча цієї сесії, але перевірено
заразом)**: сайдбар "YOUR PATH · VOCABULARY" показав "Topic 6 / 981,
dish, am, doll, 5 done · 976 to go, 0.5%" — і position-based нумерація
(Фаза 2026-08-23/24), і новий `_sidebar_lesson_name()` (показ назви
уроку замість голого id) — обидва працюють коректно на реальних даних.
Recommender-банер на екрані вибору уроку показав "Теме 100208"/"Теме
100006" (сирі id) — підтверджує вже задокументовану, свідомо відкладену
знахідку рев'ю (нумерація неконсистентна між сайдбаром і цим банером).

## My Path переосмислено: reading-first послідовність замість вільного скору (2026-08-27)

Наталя (сама репетитор) описала свою реальну методику: 2-5 уроків читання
(літери) спочатку, ОБОВ'ЯЗКОВО для повністю нульового учня — без цього
навіть найлегша граматика нечитабельна (її власний досвід із корейською:
без знання літер не розуміла найлегших фраз). Далі — граматика й лексика
по одному, потім знову 2-3 читання, і так далі, поки не пройдено; лексику
можна вперемешку, а граматику ВАЖЛИВО давати строго по порядку (кожен
наступний урок складніший за попередній навмисно) — вільний скоринг, який
recommender робив досі, цьому суперечить. Дозволяється пробувати складнішу
вправу, якщо учень встигає; якщо провалює пробу — повертатись і тренувати
пропущене. Для ko/ja/zh — окремо: поки весь набір символів (хірагана/
джамо/тони) не пройдено хоч раз, решта нечитабельна взагалі, тож для них
гейт — увесь reading-каталог мови, не фіксоване число.

**Знахідка перед реалізацією**: `scripts/seed_content_units.py::READING_LANGS`
був жорстко обмежений на `["en","uk","es","ko"]` — саме тому Наталя побачила
"Reading 0/0" на скріні для French. Але `data/reading_lessons.xlsx` вже
реально містив повноцінний, якісний контент для ще 8 мов (fr/de/pt/it/pl/
ru/ja/zh, по 30 уроків кожна) — японська й китайська вкладки вже мали
лінгвістично коректні пояснення в колонці "Правило читання" (хірагана по
рядах з поясненням складового письма, піньїнь з поясненням 4 тонів) —
просто ніколи не були засіяні в `content_units`. `reading_app.py` теж уже
мав повну підтримку всіх 12 мов (`TTS_CONFIG`/`WHISPER_LANG`/`LANG_LABELS`)
— тобто вся інфраструктура була готова заздалегідь, бракувало лише
тегування+сидування.

**Зроблено:**
- [scripts/tag_reading_new_langs.py](scripts/tag_reading_new_langs.py) —
  одноразовий, адитивний (не чіпає існуючі 273 рядки) генератор тегів для
  нових 8 мов, тим самим методом, що вже використовувався для en/uk/es/ko
  (перевірено звіркою формули з реальними межами: `level` — позиційний
  бакет, межа = `ceil(i*N/6)+1`, точно відтворює 1/15/28/41/55/68 для en,
  1/14/26/39/51/64 для ko і т.д.; `topic` — перше непорожнє значення
  "Правило читання" для цього номера уроку, як і раніше). Прогнано:
  +240 рядків (8 мов × 30), [data/reading_tags_template.csv](data/reading_tags_template.csv)
  тепер 513 рядків.
- `scripts/seed_content_units.py::READING_LANGS` розширено до всіх 12
  мов з реальним контентом (`en,uk,es,ko,fr,de,pt,it,pl,ru,ja,zh`) —
  Catalan/Dutch досі без reading-вкладки взагалі, лишається відкритим.
  Пересіяно в живу Supabase: reading 273→513 юнітів (перевірено SQL —
  по 30 на кожну з 8 нових мов).
- [engine/recommender.py](engine/recommender.py) — новий шар
  `get_path_next()` над існуючим `get_next()`/mastery/srs_state, без
  зміни самої формули скору (та все ще використовується всюди, де і
  раніше — Phase 3 підмішування слабких тем, per-module банери):
  - `_reading_progress()` — (пройдено, всього) reading-юнітів для мови.
  - `SCRIPT_GATE_LANGS = {"ko","ja","zh"}` — гейт = ВЕСЬ reading-каталог
    мови; решта мов — `READING_INTRO_COUNT=5`.
  - `_grammar_frontier_unit()` — граматика більше не скорується вільно:
    завжди наступний урок за `lesson_id`, чия mastery ще нижче
    `GRAMMAR_ADVANCE_THRESHOLD=0.5`. Це одночасно дає і сувору
    послідовність, і "повернення до пропущеного" безкоштовно — урок,
    провалений чи взагалі не спробуваний, тримає mastery низькою, тож
    фронтир на ньому й залишається, без жодного нового стану/таблиці.
  - `_interleave_path()` — після гейту, поки reading не вичерпано: 1
    граматика (фронтир) + 1 лексика + `READING_INTERLEAVE_BATCH=3`
    reading, по колу.
  - `_normal_path()` — reading вичерпано (або відсутнє для мови): фронтир
    граматики + вільний скоринг для решти, як і раніше (reading і далі
    зринає через SRS-прострочення, просто фоновим повторенням, не
    окремою фазою).
  - `get_stats()` (єдина точка входу для "My Path", `path_app.py` +
    launcher-картки `app.py`) тепер кличе `get_path_next()` замість
    голого `get_next()`.
- **Перевірено наскрізно на реальній Supabase**, одноразовий тестовий
  юзер (`reset_user` до і після): French (не-script, гейт=5) — новий
  юзер отримує 5 reading + 1 наступний, після 5 реальних спроб
  переходить у чергування (grammar:1, vocab, reading, reading, reading,
  vocab, reading... — точно за патерном); Japanese (script, гейт=30) —
  новий юзер отримує лише reading аж до всіх 30 спроб, після — одразу
  `_normal_path` (grammar:1 фронтиром, вокаб вільно, reading зникає з
  топу); mastery-gate ремедіація — 1 неправильна відповідь на
  `grammar:1` тримає фронтир на місці, 6 правильних піднімають mastery
  вище порогу й фронтир переходить на `grammar:2` (перетнуто вже після
  3-ї підряд правильної — EMA з alpha=0.25 це й передбачає).
- **Підтверджено вживу через Claude in Chrome на реальному акаунті
  Наталі** (Russian→French): "Reading 0/30" замість старого "0/0",
  лічильник "1406 lessons" (було 1376, +30 точно збігається), "Next
  lesson" картка — READING · General · Lesson 1 · A1 (зелена рамка,
  саме такий вигляд мала б пропозиція з чистого reading-гейту) — не
  клікала "Start", щоб не чіпати її реальний прогрес без прямого
  прохання.

## AI-тексти для читання після вичерпання + кнопки "Легше"/"Складніше" (2026-08-27, продовження)

Обидва пункти, залишені відкритими вище, зроблено в цій же сесії.

**AI reading practice** — коли reading для мови вичерпано (перевірено вживу
на реальному Gemini):
- [engine/gemini.py::generate_reading_passage()](engine/gemini.py) — новий
  `@_require_paid`-виклик (БЕЗ `@_cache.memoize()` — навмисно, як і
  `generate_practice_test`/`generate_lesson_construction_drill`: щоразу
  свіжий текст, не один спільний закешований назавжди). Опційний
  `grammar_topics` — підмішує нещодавно вивчену граматику в промпт, якщо
  передано. Повертає `{"title": str, "sentences": [{"target","native"}, ...]}`
  — той самий шейп, що вже усталений для construction/target_grammar drills.
  **Перевірено живим Gemini-викликом** (обхід `@_require_paid` через
  `.__wrapped__`, як завжди в цьому проєкті без логіну): French A1 з темою
  "Present tense of -er verbs" — згенерував зв'язну, дійсно A1-рівневу
  історію (habite, mange, prépare, travaille, aime, regarde, écoute — усі
  дієслова -er у теперішньому часі, саме як просив промпт) з українським
  перекладом кожного речення.
- [reading_app.py::render_setup()](reading_app.py) — новий expander
  "📖 AI reading practice", з'являється лише коли
  `len(_r_done_lids) >= len(_r_int_lessons)` (увесь куратований reading-
  каталог мови вже реально пройдено) — вибір рівня (A1-C2) + кнопка
  "Generate a new passage", результат кешується в `st.session_state` per
  (мова, рівень), не в БД (ефемерний контент, не частина каталогу). Той
  самий `try/except _gemini.PaidFeatureRequired → upsell` патерн, що й
  усюди в `grammar.py` — локальна копія `_show_upsell_reading()`, бо
  `reading_app.py` досі жодного разу не викликав `engine.gemini`. Нові
  i18n-ключі (`ai_reading_*`) додані в локальний `READING_UI`-словник
  усіх 4 його мов (uk/en/es/ko — цей словник ніколи не мав решти 10, це
  не регресія).

**Кнопки "Легше"/"Складніше"** — на картці "Next lesson" у My Path, лише
для граматики:
- [engine/recommender.py::grammar_neighbor()](engine/recommender.py) —
  сусідній урок за `lesson_id` (−1 "Easier" / +1 "Harder") у тому самому
  порядку, яким користується `_grammar_frontier_unit()`. Чисто
  launch-time override — не пише в mastery/srs_state, тому не зсуває сам
  фронтир (той рухається лише від реальної спроби, як і раніше). `None`
  на краях послідовності.
- [path_app.py::main()](path_app.py) — дві кнопки поруч із "▶ Start",
  видимі тільки коли рекомендований урок — граматика; неактивні
  (`disabled`), якщо сусіда немає (урок 1 → немає "Easier", останній
  урок → немає "Harder").
- **Перевірено на реальній Supabase**: `grammar_neighbor('French', 1, -1)`
  → `None` (немає легшого за перший), `grammar_neighbor('French', 1, +1)`
  → `grammar:2`, `grammar_neighbor('French', 2, -1)` → `grammar:1`,
  межа на останньому уроці (включно з промотованими "мовними шляхами"
  1000+) теж коректно повертає `None`.

`py_compile` проходить на всіх змінених файлах; `import` усіх чотирьох
модулів разом (`engine.recommender`, `engine.gemini`, `path_app`,
`reading_app`) — без помилок.

**Не перевірено вживу через реальний клік у браузері** (на відміну від
основної reading-first фічі вище, яку підтвердила на живому акаунті):
сам експандер "AI reading practice" і кнопки "Easier"/"Harder" — обидва
потребують або реально пройденого до кінця reading-треку (30+ уроків),
або граматичної рекомендації як поточної картки, жодного з двох станів
не було на її акаунті в момент перевірки (щойно почала з reading-гейту).
Логіка з обох боків (Gemini-функція, DB-запити) перевірена напряму й
живим викликом — лишається тільки сам UI-рендер/клік, коли вона дійде
до цих станів природним шляхом.

## Мобільний лаунчер: мова → тест → My Path → свайп-стрічка (2026-08-27, продовження)

Наталя показала скрін мобільної версії — сітка з 6 однакових карток
(Grammar/Vocabulary/Phrasebook/Reading/My Phrases/My Path) змушувала
гортати картинки, а тест на рівень був захований у collapsed expander
під нею. Її бачення: мова (як є) → тест на рівень (помітніше) → My Path
як дефолтний екран → звідти свайп вправо-вліво, щоб обрати чисто один
модуль.

**Зроблено:**
- [app.py::render_launcher()](app.py) — тест на рівень (`_render_placement_quiz`)
  піднятий одразу під вибір мови (раніше викликався в самому низу, під
  сіткою). Сітка з 6 карток прибрана повністю — замінена на одну
  "hero"-картку "My Path" (той самий `_module_progress_card`, просто для
  одного ключа) з кнопкою "▶ Continue My Path".
- [path_app.py::_render_module_swipe_strip()](path_app.py) — нова
  горизонтальна CSS scroll-snap стрічка (`overflow-x:auto;
  scroll-snap-type:x mandatory`) з картками Grammar/Vocabulary/
  Phrasebook/Reading/My Phrases, рендериться одразу під заголовком "My
  Learning Path". Це справжній тач-свайп на мобільному — чиста CSS, без
  бібліотеки жестів. Кожна картка — простий `<a href="?swipe_module=...">`
  (Streamlit-кнопки не можна вбудувати у власний HTML-блок).
- `app.py::main()` — новий, беззумовний бридж для `?swipe_module=...`
  (на відміну від загального `?module=...` sync, який заповнює лише
  ПОРОЖНІЙ `active_module` — тут потрібно перезаписати вже активний
  "path"). Той самий патерн "query-param, consumed once", що вже
  використовує `vnav_lesson` у reading_app.py/grammar.py для
  wave-nav кліків.
- **Знайдений і виправлений реальний CSS-баг під час живої перевірки**:
  картки свайп-стрічки спершу рендерились з підкресленим текстом
  (Streamlit власний `[data-testid="stMarkdownContainer"] a` правило
  переважало мій `.path-swipe-card{text-decoration:none}` — той самий
  клас/атрибут-селектор має РІВНУ вагу, елемент-селектор `a` у їхньому
  правилі додає специфічність). Виправлено — `a.path-swipe-card` +
  `!important`.

**Перевірено вживу через Claude in Chrome на реальному акаунті Наталі**
(перезапустила застряглий локальний сервер на 8501 — той самий, вже
задокументований урок цієї сесії): лаунчер — мова → тест (видимий,
не захований) → одна hero-картка "My Path" з реальним прогресом
("Юнит 6 / 1433"); клік "Continue" → реальний перехід на My Path; свайп-
стрічка рендериться коректно (без підкреслення після фіксу); клік по
картці "Reading" → реальний перехід у Reading-модуль (Lesson 1/53,
Español обраний правильно). **Побічно підтверджено та сама сесія**:
Reading для Spanish вже 5/53 (гейт пройдено) → "Next lesson" показав
ГРАМАТИКУ (Lesson 70, A2) з кнопками "Easier"/"Try harder" — reading-
гейт, чергування і easier/harder з попередньої сесії сьогодні реально
працюють разом на живих даних, не лише в ізольованих Python-тестах.

**Не досконало**: клік по картці свайп-стрічки в тестовому Chrome-
розширенні відкривав НОВУ вкладку замість навігації в тій самій (не
підтверджено як баг застосунку — посилання просте, без `target`,
найімовірніше особливість самого автоматизаційного розширення; звичайний
тап на реальному мобільному Safari/Chrome іде в тій самій вкладці).
Варто, щоб Наталя підтвердила на реальному телефоні.
